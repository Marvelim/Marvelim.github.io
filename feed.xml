<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="Marvelim.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="Marvelim.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-12T05:09:46+00:00</updated><id>Marvelim.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design.
</subtitle><entry><title type="html">Algebraic Structure and Combinatorial Mathematics</title><link href="Marvelim.github.io/blog/2026/algebra/" rel="alternate" type="text/html" title="Algebraic Structure and Combinatorial Mathematics"/><published>2026-03-24T09:00:00+00:00</published><updated>2026-03-24T09:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/algebra</id><content type="html" xml:base="Marvelim.github.io/blog/2026/algebra/"><![CDATA[<h1 id="第十五章-代数系统">第十五章 代数系统</h1>
<h2 id="运算">运算</h2>
<p><strong>$n \geq 0$ 元运算</strong>：函数 $f: A^n \to A$. 注意运算要求<strong>封闭性</strong>，即运算结果均属于 $A$.</p>
<p>对一种运算，通常使用 <strong>解析表达式</strong> 或 <strong>运算表</strong> 表示。后者对集合有穷的情况，$r_{i, j} = a_i \circ a_j$. 一元运算直接写即可。</p>
<p>特殊符号：</p>
<ul>
<li>$M_n(R)$ 代表全体实数 $n$ 阶方阵;</li>
<li>$P(B)$ 代表 $B$ 的幂集;</li>
<li>$R(B)$ 代表 $B$ 上二元关系的集合；</li>
<li>$A^A$ 代表所有 $f: A \to A$ 构成的集合。</li>
</ul>
<p><strong>单个二元运算的算律</strong>：</p>
<ul>
<li>交换律</li>
<li>结合律</li>
<li>幂等律（$a \circ a = a$）</li>
<li>消去律，即【同除】（$a \circ b = a \circ c, a \neq \theta \to b = c; b \circ a = c \circ a , a \neq \theta \to b = c$）</li>
</ul>
<p><strong>两个不同二元运算的算律</strong>:</p>
<ul>
<li>
<p>分配律：<strong>左分配</strong> $a \circ (b * c) = (a \circ b) * (a \circ c)$, <strong>右分配</strong> $(b * c) \circ a = (b \circ a) * (c \circ a)$. 简记：若运算 $A$ ”拆开了“另一种运算 $B$，则说 $A$ 分配了 $B$, 或者说 $A$ 对 $B$ 可分配。</p>
</li>
<li>
<p>吸收律：对两种 <strong>可交换</strong> 运算 $\circ$ 和 $\ast$，若有 $a \circ (a \ast b) = a$ 且 $a \ast (a \circ b) = a$，则称这两种运算有吸收律。举例：$\cap$ 和 $\cup$, $\min$ 和 $\max$.</p>
</li>
</ul>
<p><strong>二元运算特异元素</strong>：设 $\circ$ 为 $A$ 上二元运算</p>
<ul>
<li>
<p>单位元 $e$ （同时为左单位元 &amp; 右单位元）:  $\forall a \in A, a \circ e = e \circ a = a$.</p>
</li>
<li>零元 $\theta$（同时为左 &amp; 右）: $\forall a \in A, a \circ \theta = \theta \circ a = \theta$.</li>
<li>幂等元 $a$: $a \in A, a \circ a = a$.</li>
<li>可逆元 $x$ &amp; 逆元 $y$（同时为左逆 &amp; 右逆）: $x \in A, \exists y \in A,  x \circ y = y \circ x = e$.</li>
</ul>
<p><strong>说明</strong>：<strong>存在特异元素也可以作为算律</strong>（如：同一律 $\to$ 存在单位元; 零律 $\to$ 存在逆元）</p>
<blockquote>
<p><strong>定理 1（若存在左单位元和右单位元，则二者相等且为唯一单位元）</strong>： 对 $A$ 和其上的二元运算 $\circ$, 若存在 $e_l, e_r \in A$ 使得 $\forall x \in A$ 有 $e_l \circ x = x \circ e_r = x$，则 $e_l = e_r = e$ 并且 $e$ 为 $(A, \circ)$ 唯一单位元。</p>
</blockquote>
<p><strong>证明思路</strong>：【使用单位元之间运算来让其直接交互来证明相等。】具体的， $e_l$ 和 $e_r$ 直接交互有 $e_l = e_l \circ e_r = e_r$, 从而二者相等；若还存在另一单位元 $e’$, 则 $e’ = e’ \circ e = e$, 从而二者也相等。</p>
<blockquote>
<p><strong>定理 2（若存在左零元 &amp; 右零元，则二者相等且为唯一零元）</strong>： 对 $A$ 和其上的二元运算 $\circ$, 若存在 $\theta_l, \theta_r \in A$ 使得 $\forall x \in A$ 有 $x \circ \theta_l = \theta_r \circ x = \theta$，则 $\theta_l = \theta_r = \theta$ 并且 $\theta$ 为 $(A, \circ)$ 唯一零元。</p>
</blockquote>
<p><strong>证明思路</strong>：同定理 $1$.</p>
<blockquote>
<p><strong>定理 3 (对【可结合】二元运算，左逆 $=$ 右逆 = 唯一逆元）</strong>：设 $\circ$ 为 $A$ 上可结合二元运算，对 $x \in A$ 若存在 $y_l, y_r$ 使得 $x \circ y_r = y_l \circ x = e$，则 $y_l = y_r = y$ 并且 $y$ 为 $x$ 唯一逆元。</p>
</blockquote>
<p><strong>证明思路</strong>：考虑三项乘积 $y_r \circ x \circ y_l$, 这样可以同时用到结合律和左右逆的性质，从而得到 $y_l = y_r$. 此外若存在另一逆元 $y’$, 同样用 $y’ \circ x \circ y$ 结合律得到 $y’ = y$.</p>
<h2 id="代数系统">代数系统</h2>
<p>常用符号：</p>
<ul>
<li>$A$ 表示【载体】</li>
<li>$\Omega$ 表示【运算集】，满足 $\Omega = \bigcup_{j = 1}^{+\infty} \Omega_j$, $\Omega_j$ 为 $A$ 上所有 $j$ 元运算;</li>
<li>$K$ 表示代数常数集</li>
</ul>
<p><strong>代数系统（构成：成分 + 公理（即算律））的三种记法</strong> $V = \langle A, \Omega, K \rangle$ or $V = \langle A, \Omega\rangle$ or $V = \langle A, o_1, o_2, \ldots, o_r \rangle$. 例子：$\langle Z, +, \dot \rangle$, $\langle P(B), \cap, \cup \rangle$.</p>
<p><strong>代数系统的分类</strong></p>
<ul>
<li><strong>同类型</strong> 只需要运算的元数对应，也就是 $V_1 = \langle A, o_{11} \sim o_{1r} \rangle$, $V_2 = \langle B, o_{21} \sim o_{2r} \rangle$, 只需要满足 $o_{1i}$ 和 $o_{2i}$ 元数相同即可。</li>
<li><strong>同种</strong>要求这些运算的性质也相同（如：交换、幂等、结合、吸收、分配、消去），因此同种一定同类型，但同类型不一定同种。</li>
</ul>
<p><strong>子代数</strong></p>
<ul>
<li>
<p>对 $A$ 的<strong>非空子集</strong> $B$，若 $B$ 对于 $V$ 种所有运算封闭，则 $V’ = \langle B, o_1 \sim o_r\rangle$ 为 $V$ 的子代数。若 $B$ 是 $A$ 的<strong>真子集</strong>，则 $V’$ 为 $V$ 的<strong>真子代数</strong>。</p>
</li>
<li>
<p>$V$ 称作 $V$ 的<strong>平凡子代数。</strong> 此外，若代数常数集合为 $K$, 且 $K$ 也满足上述性质，那么 $\langle K, o_1 \sim o_r \rangle$ 也被称作平凡子代数。</p>
</li>
</ul>
<p><strong>同类型代数系统的积代数</strong> 对 $V_1 = \langle A, o_{11} \sim o_{1r} \rangle$, $V_2 = \langle B, o_{21} \sim o_{2r} \rangle$, $V_1 \times V_2 = \langle A \times B, o_1 \sim o_r \rangle$, 其中 $o_i$ 也是 $k_i$ 元运算，相当于分别对 pair 的第一维和第二维做运算然后合成一个 pair. 称 $V = V_1 \times V_2$ 为 $V_1, V_2$ 积代数，$V_1, V_2$ 为 $V$ 因子代数。</p>
<p>可以理解为积代数是为两个同类型代数系统提供了一个统一接口，让其可以一起运算。</p>
<blockquote>
<p><strong>注意</strong>：这里的代数系统中的运算是有序的，因为换一下可以得到另一个积代数。</p>
</blockquote>
<p>对积代数，有如下定理：</p>
<blockquote>
<p>定理：设 $V_1 = \langle A, o_{11} \sim o_{1r} \rangle$ 和 $V_2 = \langle B, o_{21} \sim o_{2r} \rangle$, $V_1$ 和 $V_2$ 积代数为 $V = \langle A \times B, o_1, o_2, \ldots, o_r \rangle$, 则：</p>
<ol>
<li>$o_{1i}, o_{2i}$ 分别在 $V_1, V_2$ 中满足交换律（结合/幂等），则 $o_i$ 满足上述性质；</li>
<li>$o_{1i}$ 对 $o_{1j}$, $o_{2i}$ 对 $o_{2j}$ 分配，则 $o_i$ 对 $o_j$ 分配。</li>
<li>$o_{1i}, o_{1j}$ 和 $o_{2i}, o_{2j}$ 分别吸收，那么 $o_i, o_j$ 吸收。</li>
<li>对 $o_{1i}$ 和 $o_{2i}$ 的单位元 / 零元，组合后依旧是 $o_i$ 的单位元 / 零元。</li>
<li>对 $o_{1i}$ 和 $o_{2i}$ 都是含单位元的运算，$a \to a^{-1}, b \to b^{-1}$, 那么 $\langle a, b \rangle$ 的逆元依旧是 $\langle a^{-1}, b^{-1} \rangle$.</li>
</ol>
</blockquote>
<p>也就是说，对位置对应的运算，几乎所有的运算律依旧满足。<strong>注意：消去律不一定满足，因为可以让其中一维度复合一个零元，但是复合之后的东西并不是积代数的零元。</strong> 如：$\langle 0, 1 \rangle \otimes \langle 1, 0 \rangle = \langle 0, 1 \rangle \otimes \langle 0, 0 \rangle$, 然而 $\langle 0, 1 \rangle$ 不是积代数的零元。</p>
<p>因此，若公理无消去律，则积代数与因子代数同种。反之，积代数<strong>不一定</strong>与因子代数同种。</p>
<h2 id="代数系统的同态与同构">代数系统的同态与同构</h2>
<p><strong>同类型的同态映射 $f: A \to B$</strong>：设 $V_1 = \langle A, o_1 \sim o_r \rangle$ 和 $V_2 = \langle B, o_1’ \sim o_r’ \rangle$ ，$f$ 满足对所有的 $(o_i, o_i’)$ 均有 $f(o_i) \equiv o_i’(f)$. 则称 $f$ 是 $V_1 \to V_2$ 的同态映射，简称同态。按映射的性质分为<strong>单同态、满同态、同构（双射）</strong>，按载体分有<strong>自同态</strong>。</p>
<p><strong>注：同态需要保持所有运算，包括 $0$ 元运算，</strong> 例如 $V = \langle A, \cdot, \begin{bmatrix} 1 &amp; 0 \ 0 &amp; 1 \end{bmatrix} \rangle$ 的自同态 $f$ 就需要满足 $f(\begin{bmatrix} 1 &amp; 0 \ 0 &amp; 1 \end{bmatrix}) = \begin{bmatrix} 1 &amp; 0 \ 0 &amp; 1 \end{bmatrix}$.</p>
<p>例子：$\langle \mathbb{Z}_n, \oplus \rangle$ 有 $n$ 个自同态，但只有 $\varphi(n)$ 个自同构。（其实只需要选择 $f(1)$ 是谁就可以了，因为 $\mathbb{Z}_n$ 是 $1$ 生成的群）</p>
<p><strong>同态性质</strong></p>
<blockquote>
<ol>
<li>同态的合成依旧为同态：即 $f: V_1 \to V_2, g: V_2 \to V_3$ 是同态，那么 $g \circ f: V_1 \to V_3$ 也是同态。</li>
</ol>
</blockquote>
<p><strong>证明思路</strong>：首先显然为映射，$f$ 是同态说明对任意运算对 $o_1, o_2$ 有 $f(o_1) = o_2(f)$. $g$ 是同态说明对任意 $o_2, o_3$ 有 $g(o_2) = o_3(g)$. 从而对运算对 $o_1, o_3$ 有</p>
\[(g \circ f)(o_1) = g(f(o_1)) = g(o_2(f)) = o_3(g(f)) = o_3(g \circ f)\]
<p>因此也是同态。</p>
<blockquote>
<p>推论：代数系统同构有自反、对称、传递性，是等价关系。</p>
</blockquote>
<blockquote>
<ol>
<li>同态像是映射到代数系统的<strong>子代数</strong>：即 $f(A)$ 关于 $V_2$ 中运算构成代数系统，并且是 $V_2$ 的子代数，称作 $V_1$ 在 $f$ 下的<strong>同态像</strong>。</li>
</ol>
</blockquote>
<p><strong>证明思路</strong>：首先说明</p>
<ol>
<li>
<p>$f(A)$ 是 $A$ 的【非空子集】，这是显然的。</p>
</li>
<li>
<p>$f(A)$ 对 $V_2$ 中所有运算封闭。要证明这点，需要讨论 $0$ 元运算和其它运算：1. 保持了所有代数常数，因此 $0$ 元运算保持；2. 对其他运算，可以求出原像，原像先运算后映射的结果也在像内，因此得证。</p>
</li>
</ol>
<p>从上面的结果可以得知，$f$ 相当于把 $V_1$ 的运算过渡到 $V_2$. 所以有一个挺有意思的性质：</p>
<blockquote>
<ol>
<li>若 $f$ 不是满同态，则 $V_2$ 中运算会保持 $V_1$ 中的【交换、结合、幂等、分配、吸收】律；同时 $f(e)$ 是 $V_2$ 中单位元，$f(\theta)$ 是 $V_2$ 中零元，$f(a^{-1}) = f(a)$ 的逆元。</li>
</ol>
</blockquote>
<p>这个结论需要注意两点：</p>
<ol>
<li>若 $f$ 不是满同态，则上述条件只在 $f(A)$ 中保持。此时，$f(A)$ 中会单独存在单位元，而 $f$ 会将单位元映射到 $f(A)$ 中单位元。一个有趣的例子是</li>
</ol>
<p><img src="image-2.png" alt="满同态"/></p>
<p>这个例子里 $f$ 就不是满同态了，但是依旧会将 $A$ 中单位元映射到 $f(A)$ 中单位元。</p>
<ol>
<li><strong>消去律</strong>不一定满足。原因是同态有可能会让信息压缩到：不同的值映射到同一个值，比如 $\mathbb{Z} \to \mathbb{Z} / 6\mathbb{Z}$. 因此消去律可能不成立。（其实这点我也没找到特别好的理解方法）</li>
</ol>
<h2 id="同余关系与同余类">同余关系与同余类</h2>
<p><strong>定义</strong> 对于一个代数系统 $V = \langle A, o_1 \sim o_r \rangle$ 和一个等价关系 $\sim$, 对 $A$ 中任意的元素 ${ a }, { b }$, 若 $\forall i, a_i \sim b_i \Rightarrow o_i(a) \sim o_i(b)$, 则称 $\sim$ 对 $o_i$ 有<strong>置换性质</strong>。若 $\sim$ 对 $V$ 中所有运算<strong>都有置换性质</strong>，则称 $\sim$ 是 $V$ 上 <strong>同余关系</strong>，称 $A$ 中等价类为 <strong>同余类</strong>。</p>
<p>显然任何代数系统都存在同余关系，比如所有元素都等价、或者所有元素都不等价。</p>
<p><strong>商代数</strong> 对 $V = \langle A, o_1 \sim o_r \rangle$ 和同余关系 $R$, 定义 $V/R = \langle A/R, \bar{o}_1 \sim \bar{o}_r \rangle$. 其中：</p>
<ul>
<li>$A/R$ 是关于同余关系的商集；</li>
<li>$\bar{o}<em>i([a_1], [a_2], \ldots, [a</em>{k_i}]) = [o_i(a_1, a_2, \ldots, a_{k_i})]$.</li>
</ul>
<p>代入真 同余关系，会觉得上述商代数和同余关系的定义都是比较自然的。不过首先要证明这是 <strong>良定义</strong> 的：也就是若 $a_j \sim b_j$, 则 $\bar{o_i}(b_j) = \bar{o_i}(a_j)$. 这是显然的，因为 $a_j \sim b_j$ 说明 $o_i(a_j) \sim o_i(b_j)$, 因此 $\bar{o}_i(a_j) =  \bar{o}_i(b_j)$.</p>
<p>有了良定义之后，就可以进一步研究商代数的性质。</p>
<blockquote>
<p>$V/R$ 满足 $V$ 的除了消去律以外的全部律；$[e]$ 是商代数单位元；$[\theta]$ 是商代数的零元；$[a]^{-1} = [a^{-1}]$.</p>
</blockquote>
<p><strong>注意消去律依旧不满足</strong>（考虑同余关系 $\mod 4$, $[2] \times [2] = [0] \times [2]$, 但 $[2] \neq [0]$.）</p>
<p>其实上述内容说明 同余 和 同态 是有一些关系的，下面的内容会进一步说明这一点。</p>
<blockquote>
<p><strong>（定理 1) 同态映射 导出 同余关系</strong>：设 $V_1 = \langle A, o_1 \sim o_r \rangle$ 和 $V_2 = \langle B, o_1’ \sim o_r’ \rangle$ ，$f: A \to B$ 是同态映射。则 $f$ 导出的 $A$ 上等价关系为 $V_1$ 上同余关系。</p>
</blockquote>
<p>直观来理解，如果拿一些 $f$ 完全相同的做运算，那么可以借助 $V_2$ 中运算的相等形式（$o’(f)$ 相等）来回来得到 $f(o)$ 相等，从而得到 $V_1$ 中运算会在一个等价类内，相当于借助 $B$ 的运算。</p>
<p>具体证明：任取一个运算 $o_i (k_i)$ 和 ${ a } \sim { b }$, $f(o_i(a_1, a_2, \ldots, a_{k_i})) = o_i’(f(a_1), f(a_2), \ldots, f(a_{k_i})) = o_i’(f(b_1), f(b_2), \ldots, f(b_{k_i})) = f(o_i(b_1, b_2, \ldots, b_{k_i}))$.</p>
<p>这个性质说明，每个同态都可以导出一个同余关系。但需要注意的是，并不是所有的关系都可以由自同态导出。比如 $0 \to 2, 1 \to 3, 2 \to 1, 3 \to 0$. (P85)</p>
<blockquote>
<p><strong>(定理 2) 商代数是原代数的同态像</strong>：设 $V = \langle A, o_1 \sim o_r \rangle$ 和 $R$ 是 $V$ 上的同余关系。则考虑自然映射 $g: g(a) = [a]$, 是 $V \to V/R$ 的同态映射。</p>
</blockquote>
<p>这是显然的，因为同态只要求保运算就可以了，而同余类根据定义能满足这一性质。</p>
<p>具体证明：对每个运算 $o_i (k_i)$, 只需证明 $o_i’(g(a_1), g(a_2), \ldots, g(a_{k_i})) = g(o_i(a_1, a_2, \ldots, a_{k_i}))$. 而将 $g(a)$ 写成 $[a]$ 后会发现这是商代数的定义：$o_i’([a_1], [a_2], \ldots, [a_{k_i}]) = [o_i(a_1, a_2, \ldots, a_{k_i})]$.</p>
<blockquote>
<p><strong>(定理 3) 同态基本定理，同态导出的商代数同构于同态像</strong> 若 $V_1 = \langle A, o_1 \sim o_r \rangle$ 和 $V_2 = \langle B, o_1’ \sim o_r’ \rangle$ 是同类型代数系统，$f: A \to B$ 是同态映射，$R$ 是 $f$ 导出的 $V_1$ 同余关系，则 $V_1$ 上的同余关系 $R$ 导出的 <strong>商代数</strong> $V_1/R$ 和 $\langle f(A), o_1’ \sim o_r’ \rangle$ 是 <strong>同构</strong> 的。</p>
</blockquote>
<p>定义映射 $h([a]) = f(a)$ 即可（看起来大小是一样的就很同构），后续要依次验证<strong>良定义</strong>，<strong>双射</strong>和<strong>同态映射</strong>三点。</p>
<p>很别扭的一点是要验证 $0$ 元运算，考虑若 $[a] \in V_1/R$ 是零元运算，那么按照商空间的定义 $a$ 在 $V_1$ 中是零元运算，因此 $f(a)$ 也是 $f(A)$ 中零元运算（可以参考之前讲的 $f$ 【保零元运算】）</p>
<h1 id="第十六章-半群与独异点">第十六章 半群与独异点</h1>
<p><strong>定义</strong></p>
<ul>
<li>广群：封闭二元运算</li>
<li>半群 $\langle S, \circ \rangle$：封闭二元运算 + 结合律</li>
<li>独异点（幺半群） $\langle S, \circ, e\rangle$：封闭二元运算 + 结合律 + 单位元</li>
</ul>
<p>注意：任何半群都可以通过扩张单位元的方式变为独异点；表达式中运算符可以省略。</p>
<p>在半群上可以归纳定义<strong>幂运算</strong>：$a^1 = a, a^{n+1} = a^na$. 在独异点可以补充定义 $a^0 = e$. 由二元运算<strong>结合律</strong>，有</p>
<blockquote>
<p>(定理 1) 关于幂运算的性质： $a^na^m = a^{n+m}, (a^n)^m = a^{nm}$.</p>
</blockquote>
<p><strong>子半群 $\langle B, \circ \rangle$</strong> 是半群 $\langle S, \circ \rangle$ 的一个子集，满足 $\circ$ 在 $\emptyset \neq B \subseteq S$ 中封闭。同样可以定义子独异点 $\langle B, \circ, e’\rangle$. <strong>注意：子独异点必须继承独异点的单位元。</strong> 有如下定理：</p>
<blockquote>
<p>(定理 2) 若干子半群的非空交集仍为子半群，若干子独异点的交集仍为子独异点。</p>
</blockquote>
<p>证明比较显然，反证即可。</p>
<p>对任意子集 $B \subseteq S$, 可以定义 $B$ 的<strong>生成群</strong> $\langle B \rangle$ 为最小的包含 $B$ 的半群。而这有两种定义方式：</p>
<ol>
<li>
<table>
<tbody>
<tr>
<td>借助 <strong>扩张</strong> 的想法，用 $B$ 中元素做运算直到无法扩张位置，即 $\langle B \rangle = \bigcup_{n \in Z^+} B^n, B^n = { b_1 b_2 \cdots b_n \</td>
<td>b_i \in B }$.</td>
</tr>
</tbody>
</table>
</li>
<li>借助 <strong>子半群的交集</strong> 的想法，即 $\langle B \rangle = \bigcap { A \mid B \subseteq A}$ , 其中 $A$ 是 $S$ 的子半群。</li>
</ol>
<h1 id="第十七章-群">第十七章 群</h1>
<p><strong>定义</strong></p>
<ol>
<li>可以将群看作代数系统 $\langle G, \circ, ^{-1}, e \rangle$. 定义<strong>非空集合</strong> $G$ 上的<strong>二元运算</strong> $\circ$ 满足：
<ul>
<li>结合律：对任意 $a, b, c \in G$, $(a \circ b) \circ c = a \circ (b \circ c)$.</li>
<li>单位元：$\exists e \in G$，s.t. $\forall a \in G$，有 $e \circ a = a \circ e = a$.</li>
<li>逆元：对每个 $a \in G$，存在 $a^{-1} \in G$，s.t. $a \circ a^{-1} = a^{-1} \circ a = e$.</li>
</ul>
</li>
</ol>
<blockquote>
<p>然而这个定义并不是最简单的定义，其实只需要存在右单位元 $e$ ，并且每个元素 $a$ 都存在右逆元 $a^{-1}$ 即可。</p>
</blockquote>
<p><strong>证明</strong>：先证明 $e$ 是左单位元，即对任何 $a$ 有 $ea = a$. 对此，两侧同乘 $a^{-1}$ 只需证 $eaa^{-1} = aa^{-1}$（可以两侧 $\times a$ 消掉），相当于只需证 $ee = e$, 而由于 $e$ 是右逆这是显然的。</p>
<p>再证明 $a^{-1}$ 是 $a$ 的左逆元，即 $a^{-1}a = e$. 对此，只需证 $aa^{-1}a = ae$, 从而只需证明 $ea = ae$, 而这由 $e$ 是左单位元是显然的。</p>
<table>
<tbody>
<tr>
<td>对一个群，可以定义其<strong>阶</strong>为群的基数（有限群记为 $</td>
<td>G</td>
<td>$），同样也可以定义元素 $a$ 的 $n$ 次幂：</td>
</tr>
</tbody>
</table>
\[a^n = \begin{cases}
e, n = 0 \\
a^{n-1} a, n &gt; 0 \\
(a^{-1})^m, m = -n, n &lt; 0
\end{cases}\]
<p>特别的，对任何一个元素 $a$, 定义其「阶」为<strong>使得 $a^k = e$ 的最小正整数 $k$</strong>. 若不存在这样的 $k$, 则称其阶为 $\infty$.（例如：$\langle \mathbb{Z}, + \rangle$）对于有限群，其阶肯定是有限的（特别的，阶是群的阶的因子）。但是若每个元素都是有限阶的，那么群的阶不一定是有限的，比如所有单位根和乘法构成的群。（可列 和 有限 不一样）</p>
<p>对幂运算有如下规则：</p>
<blockquote>
<p>$(a^{-1})^{-1} = a, (ab)^{-1} = b^{-1}a^{-1}, a^na^m = a^{n+m}, (a^n)^m = a^{nm}$</p>
</blockquote>
<p>让我们看一下下面的定理</p>
<blockquote>
<p><strong>定理 3</strong> $ax=b$ 和 $ya=b$ 在群 $G$ 中有解且有唯一解。</p>
</blockquote>
<p>证明：只考虑 $ax=b$. 首先 $x=a^{-1}b$ 一定是一组解。若还有别的解 $c$ 满足 $ac=b$, 那么两侧同乘 $a^{-1}$ 有 $c=a^{-1}b$.</p>
<blockquote>
<p><strong>定理 4</strong> 对半群 $G$, 若对任意 $a, b \in G$ 有 $ax=b$ 和 $ya=b$ 在 $G$ 中有解，则 $G$ 为群。</p>
</blockquote>
<p>证明：只需要证明有 <strong>右单位元</strong> 和每个元素的 <strong>右逆元</strong>。这里任取一个元素 $b$, 用 $bx=b$ 的解 $x$ 当作 $e$.</p>
<p>那么对任意 $a$ 可以将其分解为 $a = cb$（由于 $?b = a$ 有解），从而 $ae = cbe = cb = a$, 从而 $e$ 是右单位元。然后由 $ax=e$ 均有解和 $a$ 的任意性，得到每个元素均有右逆元。</p>
<blockquote>
<p><strong>定理 5（消去律）</strong> $ab=ac \Rightarrow b=c$, $ba=ca \Rightarrow b=c$.</p>
</blockquote>
<p>显然，每个元素有逆元，这也证明了群里没有零元。</p>
<blockquote>
<p><strong>定理 6</strong> 对有限半群 $G$, 若 $G$ 不含零元且消去律成立，则 $G$ 是群。</p>
</blockquote>
<p>证明：<strong>试图调用定理 4.</strong> 有限的好处是可以将所有可能的乘积全列出来，也就是对于 $a_i$ 我们定义 $a_i G = { a_i a_j \mid j = 1 \sim n}$. 不过根据消去律 $a_i G$ 中元素两两不同且封闭，因此 $a_i G = G$, 因此对于任意 $a_j$ 一定存在 $a_k$ 使得 $a_i a_k = a_j$. 同理可以证明一定存在 $a_k$ 使得 $a_k a_i = a_j$, 从而 $G$ 是群。</p>
<p>从上面定理 6 可以知道下面的定理 7:</p>
<blockquote>
<p><strong>定理 7</strong> 若 $G$ 是群，则 $\forall a\in G,aG=G, Ga=G$</p>
</blockquote>
<p>不过反之不一定成立，因为运算要满足结合律。</p>
<blockquote>
<table>
<tbody>
<tr>
<td><strong>定理 8</strong> 若 $G$ 是群，且 $</td>
<td>a</td>
<td>= r$, 则 1.  $a^k = e \Rightarrow r</td>
<td>k$; 2. $</td>
<td>a</td>
<td>=</td>
<td>a^{-1}</td>
<td>$; 3. 若 $</td>
<td>G</td>
<td>= n$, 则 $r \leq n$.</td>
</tr>
</tbody>
</table>
</blockquote>
<p>证明：反证法证明即可。</p>
<p>有一些有关于阶的重要结论：</p>
<blockquote>
<ol>
<li>
<table>
<tbody>
<tr>
<td>$</td>
<td>a</td>
<td>= 1,2 \Leftrightarrow a=a^{-1}$</td>
</tr>
</tbody>
</table>
</li>
<li>
<table>
<tbody>
<tr>
<td>$</td>
<td>a</td>
<td>=</td>
<td>a^{-1}</td>
<td>,</td>
<td>ab</td>
<td>=</td>
<td>ba</td>
<td>,</td>
<td>a</td>
<td>=</td>
<td>bab^{-1}</td>
<td>$, 因此将 $bab^{-1}$ 称作 $a$ 的一个<strong>共轭元</strong>。</td>
</tr>
</tbody>
</table>
</li>
<li>
<table>
<tbody>
<tr>
<td>$</td>
<td>a</td>
<td>= r \Rightarrow</td>
<td>a^t</td>
<td>= \frac{r}{(t, r)}$</td>
</tr>
</tbody>
</table>
</li>
<li>
<table>
<tbody>
<tr>
<td>$</td>
<td>a</td>
<td>= n,</td>
<td>b</td>
<td>= m, ab = ba \Rightarrow</td>
<td>ab</td>
<td> </td>
<td>[n, m]$. 若 $(n, m) = 1$, 则 $</td>
<td>ab</td>
<td>=nm$.</td>
</tr>
</tbody>
</table>
</li>
</ol>
</blockquote>
<p>有一些算阶的套路：</p>
<blockquote>
<table>
<tbody>
<tr>
<td>$</td>
<td>x</td>
<td>=</td>
<td>y</td>
<td>$ 证明：设 $</td>
<td>x</td>
<td>= r,</td>
<td>y</td>
<td>= s$, 分别证明 $x^s = e$ 和 $y^r = e$ 导出 $r</td>
<td>s, s</td>
<td>r$ 即可。</td>
</tr>
<tr>
<td>求 $</td>
<td>x</td>
<td>$: 构造 $n$ 使得 $x^n = e$,分析 $n$ 的因子。</td>
<td> </td>
<td> </td>
<td> </td>
<td> </td>
<td> </td>
<td> </td>
<td> </td>
<td> </td>
</tr>
</tbody>
</table>
</blockquote>]]></content><author><name></name></author><category term="courses"/><category term="math"/><summary type="html"><![CDATA[北京大学 代数结构与组合数学 笔记]]></summary></entry><entry><title type="html">Computer Networks Lab 2</title><link href="Marvelim.github.io/blog/2026/computer-networks-lab-2/" rel="alternate" type="text/html" title="Computer Networks Lab 2"/><published>2026-03-24T09:00:00+00:00</published><updated>2026-03-24T09:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/computer-networks-lab-2</id><content type="html" xml:base="Marvelim.github.io/blog/2026/computer-networks-lab-2/"><![CDATA[<h1 id="提权重">提权重</h1>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo chmod</span> <span class="nt">-R</span> 777 <span class="nb">.</span>
</code></pre></div></div>
<h1 id="request-and-response">Request and Response</h1>
<p>目标：发一个 request 之后 recv 一个 response. 学习 socket 用法：</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">ssize_t</span> <span class="nf">send</span><span class="p">(</span><span class="kt">int</span> <span class="n">sockfd</span><span class="p">,</span> <span class="k">const</span> <span class="kt">void</span> <span class="o">*</span><span class="n">buf</span><span class="p">,</span> <span class="kt">size_t</span> <span class="n">len</span><span class="p">,</span> <span class="kt">int</span> <span class="n">flags</span><span class="p">);</span> <span class="c1">// len 代表实际发送长度</span>
<span class="kt">ssize_t</span> <span class="nf">recv</span><span class="p">(</span><span class="kt">int</span> <span class="n">sockfd</span><span class="p">,</span> <span class="kt">void</span> <span class="o">*</span><span class="n">buf</span><span class="p">,</span> <span class="kt">size_t</span> <span class="n">len</span><span class="p">,</span> <span class="kt">int</span> <span class="n">flags</span><span class="p">);</span> <span class="c1">// len 代表最大接收长度（缓冲区大小）</span>
</code></pre></div></div>
<p>由于 rbuf 只有 RECV_SIZE 大小，因此 recv 时不能超过 RECV_SIZE.</p>
<p>感觉 recv 应该是能收全的，发的话搞成循环发试一下。</p>
<h1 id="establish-connection">Establish Connection</h1>
<p>使用 getaddrinfo 可以将域名转换为 IP 地址：</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">getaddrinfo</span><span class="p">(</span>
    <span class="k">const</span> <span class="kt">char</span> <span class="o">*</span><span class="n">node</span><span class="p">,</span> <span class="c1">// 域名</span>
    <span class="k">const</span> <span class="kt">char</span> <span class="o">*</span><span class="n">service</span><span class="p">,</span> <span class="c1">// 端口号 or 服务名</span>
    <span class="k">const</span> <span class="k">struct</span> <span class="n">addrinfo</span> <span class="o">*</span><span class="n">hints</span><span class="p">,</span>  <span class="c1">// 筛选条件</span>
    <span class="k">struct</span> <span class="n">addrinfo</span> <span class="o">**</span><span class="n">res</span> <span class="p">);</span>
</code></pre></div></div>
<p>需要注意 hint 的写法，目前是 IPv4，因此 family 是 AF_INET. 同时 socktype 是 SOCK_STREAM, 因为 SMTP 是基于 TCP 的。</p>
<p>然后用 connect 函数：</p>
<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">connect</span><span class="p">(</span><span class="kt">int</span> <span class="n">sockfd</span><span class="p">,</span> <span class="k">const</span> <span class="k">struct</span> <span class="n">sockaddr</span> <span class="o">*</span><span class="n">addr</span><span class="p">,</span> <span class="n">socklen_t</span> <span class="n">addrlen</span><span class="p">);</span>
</code></pre></div></div>
<p>需要注意的是要把 addr 转回 sockaddr, 是可以强行转换的。</p>
<h1 id="prepare-data">Prepare Data</h1>
<p>这好像比较简单？反复用 sprintf 和 strcat 即可。需要注意的是 attach_file 怎么写，用 uuencode.</p>
<h1 id="send-email">Send Email</h1>
<p>就是一个个 section 发。</p>]]></content><author><name></name></author><category term="courses"/><category term="computer_networks"/><summary type="html"><![CDATA[计算机网络（春季）Lab 2 Solution]]></summary></entry><entry><title type="html">Grade Standard</title><link href="Marvelim.github.io/blog/2026/grade/" rel="alternate" type="text/html" title="Grade Standard"/><published>2026-03-22T05:00:00+00:00</published><updated>2026-03-22T05:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/grade</id><content type="html" xml:base="Marvelim.github.io/blog/2026/grade/"><![CDATA[<h1 id="毛概">毛概</h1>
<ul>
<li>考勤 $10$ 分（$3 \sim 4$ 次柔性考勤方式）；</li>
<li>讨论 $10$ 分（pre）</li>
<li>论文 $30$ 分（$5000 \sim 8000$ 字）</li>
<li>期末 $50$ 分</li>
</ul>
<h1 id="代组">代组</h1>
<ul>
<li>平时成绩 $40 \sim 50$ 分（考勤、作业、期中考试）</li>
<li>期末考试 $60 \sim 50$ 6.24 下午</li>
</ul>
<h1 id="英语">英语</h1>
<ul>
<li>考勤 &amp; 参与 $15$ 分</li>
<li>短写作业 $20$ 分</li>
<li>口头 presentation $15$ 分</li>
<li>项目 portfolio $50$ 分</li>
</ul>
<h1 id="计网">计网</h1>
<ul>
<li>实验：$40$ 分</li>
<li>期末考试：$60$ 分（$5 \sim 10$ 分与实验课相关）</li>
</ul>]]></content><author><name></name></author><category term="courses"/><summary type="html"><![CDATA[北京大学大三第二学期课程考评标准]]></summary></entry><entry><title type="html">MIT Course 6.S184</title><link href="Marvelim.github.io/blog/2026/mit-6-s184/" rel="alternate" type="text/html" title="MIT Course 6.S184"/><published>2026-03-16T10:00:00+00:00</published><updated>2026-03-16T10:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/mit-6-s184</id><content type="html" xml:base="Marvelim.github.io/blog/2026/mit-6-s184/"><![CDATA[<h1 id="flow-model-和-diffusion-model">Flow Model 和 Diffusion Model</h1>
<h2 id="flow-model">Flow Model</h2>
<h3 id="前置知识-ode">前置知识: ODE</h3>
<p><strong>ODE 的定义与解</strong> 一个 ODE 被一个和 <strong>时间</strong> 相关的 <strong>向量场</strong> $u$ 定义，其中 $u_t(x)$ 接收“当前坐标 $x$”和“当前时间 $t$“，吐出应当走的方向 $u_t(x)$:</p>
\[u: \mathbb{R}^d \times[0,1] \rightarrow \mathbb{R}^d, \quad(x, t) \mapsto u_t(x),\]
<p>该 ODE 的解是一条<strong>轨迹</strong> $X$:</p>
\[X:[0,1] \rightarrow \mathbb{R}^d, \quad t \mapsto X_t\]
<p>该轨迹是在 ODE 上走的，因此满足</p>
\[\begin{aligned}
\frac{\mathrm{d}}{\mathrm{~d} t} X_t &amp; =u_t\left(X_t\right) \\
X_0 &amp; =x_0
\end{aligned}
\tag{1}\label{eq:ode}\]
<p>为方便，定义一个<strong>流</strong> $\psi$ 是从指定的一点 $x_0$ 到上述轨迹的映射，可以看作是接收 $x_0$，吐出整条轨迹的函数：</p>
\[\begin{aligned}
\psi : \mathbb{R}^d \times[0,1] \mapsto \mathbb{R}^d&amp; , \quad (x_0,t) \mapsto \psi_t(x_0) \\
\frac{\mathrm{d}}{\mathrm{~d} t} \psi_t(x_0) &amp;= u_t(\psi_t(x_0)) \\
\psi_0(x_0) &amp;= x_0
\end{aligned}\]
<p>若直接对不同的 $t$ 输出空间中 $\psi_t$，可以较为直观的看到其“运动”是遵循 $u_t$ 的：</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260316191317686.png" alt=""/></p>
<p><strong>ODE 的采样</strong>: 对式 \eqref{eq:ode} 使用 Euler 法可以将 ${ X_t }$ 进行离散采样：</p>
\[X_{t + h} = X_t + hu_t(X_t)\]
<p><strong>Flow Model 的定义</strong> 一个 Flow Model 是一个神经网络向量场 $u_t^{\theta}$, 满足：</p>
<ul>
<li>当 $X_0 \sim p_{\text{init}}$ 时，按该向量场采样得到的 $X_1 \sim p_{\text{data}}$.</li>
</ul>
<p>若能构造出这样的向量场，从源分布中采样一个 $X_0$ 后模拟得到 $X_1$ 即可完成从 $p_{\text{data}}$ 中采样的目的。</p>
<h2 id="diffusion-model">Diffusion Model</h2>
<h3 id="前置知识-sde">前置知识: SDE</h3>
<p>SDE 中引入的随机性可以用布朗运动来建模：一个布朗运动 $W = (W_t)_{0 \leq t \leq 1}$ 是一个随机过程，满足：</p>
<ol>
<li>
<p>$W_0 = 0$, $W_t$ 关于 $t$ 连续；</p>
</li>
<li>
<p>对任何 $s&lt;t$, $W_t - W_s \sim \mathcal{N}(0, (t - s)I_d)$;</p>
</li>
<li>
<p>对任何分割 $0 \leq t_0 &lt; t_1 &lt; \ldots &lt; t_n \leq 1$, $W_{t_1 - t_0}, \ldots, W_{t_n - t_{n-1}}$ 是独立的随机变量。</p>
</li>
</ol>
<p>布朗运动又被叫做维纳过程。幸运的是，如果按下面的流程进行离散模拟，那么就可以“近似“为一个满足上述条件的布朗运动：</p>
\[W_{t + h} = W_t + \sqrt {h} \epsilon_t, \epsilon_t \sim \mathcal{N}(0, I_d)\]
<p>大概效果如同</p>
<p><img src="image-1.png" alt="Brownian Motion"/></p>
<p><strong>ODE $\to$ SDE</strong> 如果要在 ODE 中引入上述随机性，首先求导不能用了，需要一个离散的解释。对 \eqref{eq:ode} 可以改写为</p>
\[\frac{1}{h}(X_{t + h} - X_t)  =u_t\left(X_t\right) + R_t(h)\]
<p>只需要多出来的一个项可以忽略：$R_t(h) \to 0 (h \to 0)$. 这样 reformat 可以得到</p>
\[X_{t + h} = X_t + hu_t(X_t) + hR_t(h)\]
<p>在这个表达式中添加布朗运动后：</p>
\[X_{t+h}=X_t+\underbrace{h u_t\left(X_t\right)}_{\text {deterministic }}+\sigma_t \underbrace{\left(W_{t+h}-W_t\right)}_{\text {stochastic }}+\underbrace{h R_t(h)}_{\text {error term }}\]
<p>其中 $\sigma_t$ 控制了随机项的强度。写成连续形式是</p>
\[d X_t=u_t\left(X_t\right) d t+\sigma_t d W_t\]
<p>其中，$u_t(X_t)$ 被叫做 <strong>drift coefficient</strong>，$\sigma_t$ 被叫做 <strong>diffusion coefficient</strong>.</p>
<p><strong>SDE 的采样</strong>: 使用 Euler-Maruyama Method 进行离散采样，核心是利用 $W_{t+h} - W_t$ 服从正态分布的性质，可以离散的写成：</p>
\[X_{t+h}=X_t+h u_t\left(X_t\right)+\sqrt{h} \sigma_t \epsilon_t, \quad \epsilon_t \sim \mathcal{N}\left(0, I_d\right)\]
<p><strong>Diffusion Model 的定义</strong> 一个 Diffusion Model 是一组 $(u_t^{\theta}, { \sigma_t })$，类比 Flow Model.</p>
<h1 id="流匹配">流匹配</h1>
<p>那么，该如何训练满足上述要求的 Flow Model $u_t^{\theta}$ 呢？</p>
<p>可以发现直接建模 “从 $p_{\text{init}}$ 走到 $p_{\text{data}}$” 是较为困难的，需要在中间过程想象 $u_t$ “发生了什么” 才可以精细化的设计 $u_t$. 为此，可以想象粒子从 $\text{init}$ 逐渐走向 $\text{data}$ 的路径，和其中间所经历的<strong>概率分布</strong>。下面会逐渐发现，这一概率分布会直接让 $u_t$ 的计算和训练成为可能。</p>
<h2 id="工具概率路径">工具：概率路径</h2>
<p>我们定义<strong>概率路径</strong>这一概念。</p>
<p><strong>条件概率路径</strong> 先固定一个数据点 $z \in p_{\text{data}}$, 可以想象的是，可以构造出一条<strong>条件概率路径</strong> $p_t(x \mid z)$（换句话说，就是对不同的 $t$ 都有一个 condition $z$ 的条件分布），满足</p>
\[p_0(\cdot \mid z) = p_{\text{init}}, \quad p_1 (\cdot \mid z) = \delta_z \quad \forall z \in \mathbb{R}^{d}\]
<p>也就是说这个条件概率路径可以看作是分布上的一个轨迹，可以逐渐的把初始分布 $p_{\text{init}}$ 调整为一个单点分布 $\delta_z$. 下方的图，第一行就显示了一条条件概率路径：</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260316231041302.png" alt=""/></p>
<p><strong>边际概率路径</strong> 如果按 $p_{\text{data}}(z)$ 这一分布“综合”上面的条件概率路径会发生什么？</p>
\[p_t(x) = \int_{z} p_t(x \mid z) p_{\text{data}} (z) dz\]
<p>这一分布满足 $p_0 = p_{\text{init}}, p_1 = p_{\text{data}}$. 因此，如果能够设计一个边际向量场，满足按这个向量场走之后，对每时刻 $t$，随机变量 $X_t$ 都恰好符合分布 $p_t$，那么这个向量场就可以完成 $p_{\text{init}} \to p_{\text{data}}$ 的转化了。</p>
<h2 id="条件--边际向量场">条件 &amp; 边际向量场</h2>
<p>如果构造出了上面的一个条件概率路径，能不能构造一个对应的 ODE $u_t^{\text{target}} (\cdot \mid z)$, 满足若 $X_0$ 从初始分布中采样，则 $X_t$ 时刻服从条件概率路径在 $t$ 时刻的分布呢？也就是</p>
\[X_0 \sim p_{\text {init }}, \quad \frac{\mathrm{d}}{\mathrm{~d} t} X_t=u_t^{\text {target }}\left(X_t \mid z\right) \quad \Rightarrow \quad X_t \sim p_t(\cdot \mid z) \quad(0 \leq t \leq 1) .\]
<p>这个 ODE 完成的是 $p_{\text{init}} \to \delta_z$ 的转化，有什么用呢？事实上，借用下面的 <strong>边际化技巧</strong>，可以得到我们所需要的边际向量场和上面的条件向量场之间的关系：</p>
<blockquote>
<p><strong>边际化技巧</strong>：定义 \(\begin{aligned} u_t^{\text {target }}(x)&amp;=\int u_t^{\text {target }}(x \mid z) \frac{p_t(x \mid z) p_{\text {data }}(z)}{p_t(x)} \mathrm{d} z \\ &amp;= \mathbb{E}_{z \sim p_{\text{data}}(\cdot \mid x)} [ u_t^{\text{target}}(x \mid z)] \end{aligned} \tag{2} \label{eq:margin}\)，这个向量场满足<br/>
\(\begin{aligned}
X_0 \sim p_{\text {init }}, \quad \frac{\mathrm{d}}{\mathrm{~d} t} X_t=u_t^{\operatorname{target}}\left(X_t\right) \quad \Rightarrow \quad X_t \sim p_t \quad(0 \leq t \leq 1) .
\end{aligned}\)</p>
</blockquote>
<p>这一技巧的证明较为复杂，这里略过。但若是注意到</p>
\[\frac{p_t(x \mid z) p_{\text {data }}(z)}{p_t(x)} = p_{\text{data}} (z \mid x)\]
<p>也就是实际上 $u_t^{\text{target}}(x)$ 是 $u_t^{\text{target}}(x \mid z)$ 按 $p_{\text{data}}(z \mid x)$ 的【加权平均】（或者说是”期望“），就可以在直观意义上理解这个式子。（内心戏：对 $x$ 先猜“可能的 $z$ 是什么“，对最可能的 $z$ 用多一点 $u_t(x \mid z)$）</p>
<p>这一技巧的重要性在于，只要构造出 $u_t^{\text{target}}(x \mid z)$, 就相当于变相知道了 $u_t^{\text{target}}(x)$ 是什么。因此，对一般的概率分布间的转化，可以遵循 <strong>先建模条件概率路径，后建模条件向量场</strong> 的两步走策略先算出一些量出来。实践一下：</p>
<h2 id="例题-1-p_textinit--mathcaln0-i_d--p_textdata">例题 1: $p_{\text{init}} = \mathcal{N}(0, I_d),\  p_{\text{data}}$</h2>
<p><strong>条件概率路径</strong> 若 $p_{\text{init}} = \mathcal{N}(0, I_d)$，则对一个数据点 $z$, 可以构造 $p_t(\cdot \mid z): \mathcal{N}(\alpha_t z, \beta_t^2 I_d)$. 若满足 $\alpha_0 = \beta_1 = 0, \alpha_1 = \beta_0 = 1$, 那么上述分布就满足 $p_0(\cdot \mid z) = \mathcal{N}(0, I_d), p_1(\cdot \mid z) = \mathcal{N}(z, 0) = \delta_z$，从而就是所需的条件概率路径。</p>
<p><strong>条件概率路径 $\to$ 条件流</strong> 在试图构造 $u_t^{\text{target}}(x \mid z)$ 之前，可以试图先构造出 $X_t = \psi_t(X_0 \mid z)$ 应当满足的形式。这样的话，对其关于 $t$ 求导，就可以求得向量场的形式了。<del>瞪眼可得</del></p>
\[\psi_t^{\text{target}}(x \mid z) = \alpha_t x + \beta_t z\]
<p>该流恰好满足了在任何时刻 $t$, 随机变量 $X_t$ 都服从 $p_t^{\text{target}}(x \mid z)$.</p>
<p><strong>条件向量场</strong> 对上式关于 $t$ 求导并化简，就可以得到 $u_t^{\text{target}}(x \mid z)$ 的形式：</p>
\[\begin{aligned}
&amp; \frac{\mathrm{d}}{\mathrm{~d} t} \psi_t^{\text {target }}(x \mid z)=u_t^{\text {target }}\left(\psi_t^{\text {target }}(x \mid z) \mid z\right) \quad \text { for all } x, z \in \mathbb{R}^d \\
&amp; \stackrel{(i)}{\Leftrightarrow} \quad \dot{\alpha}_t z+\dot{\beta}_t x=u_t^{\text {target }}\left(\alpha_t z+\beta_t x \mid z\right) \quad \text { for all } x, z \in \mathbb{R}^d \\
&amp;\stackrel{(i i)}{\Leftrightarrow} \quad \dot{\alpha}_t z+\dot{\beta}_t\left(\frac{x-\alpha_t z}{\beta_t}\right)=u_t^{\text {target }}(x \mid z) \quad \text { for all } x, z \in \mathbb{R}^d\\
&amp;\stackrel{(\mathrm{iiii})}{\Leftrightarrow} \quad\left(\dot{\alpha}_t-\frac{\dot{\beta}_t}{\beta_t} \alpha_t\right) z+\frac{\dot{\beta}_t}{\beta_t} x=u_t^{\mathrm{target}}(x \mid z) \quad \text { for all } x, z \in \mathbb{R}^d
\end{aligned}\]
<p>其中 $\dot{\alpha}_t = \partial_t \alpha_t, \dot{\beta}_t = \partial_t \beta_t$. 从而就得到了条件向量场：</p>
\[u_t^{\text {target }}(x \mid z)=\left(\dot{\alpha}_t-\frac{\dot{\beta}_t}{\beta_t} \alpha_t\right) z+\frac{\dot{\beta}_t}{\beta_t} x\]
<p>值得提及的是，上面的推导中并没有用到 $p_{\text{data}}$ 的任何信息（因为此处算的仅仅是“条件”），而这个信息在定义边际概率路径与边际向量场中是必要的。</p>
<h2 id="例题-2-p_textsimple-p_textdata">例题 2: $p_{\text{simple}}, p_{\text{data}}$</h2>
<p><strong>条件概率路径</strong> 如果初始分布是<strong>任意</strong>分布 $p_{\text{simple}}$, 一样可以构造出其和 $p_{\text{data}}$ 中的条件概率路径。只不过这个条件概率路径 $p_t(x \mid z)$ 的定义不是显式的：定义 $X_t = (1-t)X_0 + tz$, 那么 $X_t$ 满足的分布自然就是 $p_t (x \mid z)$. 也就是说流的形式是 $\psi_t^{\text{target}}(x \mid z) = (1-t)x + tz$.</p>
<p>对这个项关于 $t$ 求导为 $z - x$，而这个值应当等于 $u_t^{\text{target}}((1-t)x + tz \mid z)$. 因此做换元 $x’ = (1 - t)x + tz$, 可以得到</p>
\[u_t^{\text{target}}(x' \mid z) = z - \frac{x' - tz}{1 - t} = \frac{z - x'}{1 - t} ( = z-x)\]
<p>此例中是先建模条件流，后通过条件流建模条件概率路径。这说明概率路径和流本质是非常相关的。</p>
<p>（直接对这个目标做 Flow Matching 就是 Rectified Flow 了，同时这也说明 Rectified Flow 并不依赖初始分布的形态和性质。）</p>
<h2 id="学习边际向量场">学习边际向量场</h2>
<p>回到主线。<strong>边际化技巧</strong> \eqref{eq:margin} 说明，$u_t^{\text{target}} (x)$ 是可以由条件向量场“加权平均”得到的。而 Flow Model 的训练目标就是让 $u_t^{\theta} \approx u_t^{\text{target}}$. 因此可以定义损失函数为：</p>
\[\begin{aligned}
\mathcal{L}_{\mathrm{FM}}(\theta) &amp; =\mathbb{E}_{t \sim \text { Unif }, x \sim p_t}\left[\left\|u_t^\theta(x)-u_t^{\text {target }}(x)\right\|^2\right] \\
&amp; \stackrel{(i)}{=} \mathbb{E}_{t \sim \text { Unif }, z \sim p_{\text {data }}, x \sim p_t(\cdot \mid z)}\left[\left\|u_t^\theta(x)-u_t^{\text {target }}(x)\right\|^2\right],
\end{aligned}\]
<p>问题来了，$u_t^{\text{target}}(x)$ 是由一个积分定义的，不知道其具体的值怎么办？考虑下面这个形式的 loss, 对比上面的函数是把边际向量场替换为了条件向量场：</p>
\[\mathcal{L}_{\mathrm{CFM}}(\theta)=\mathbb{E}_{t \sim \mathrm{Unif}, z \sim p_{\text {data }}, x \sim p_t(\cdot \mid z)}\left[\left\|u_t^\theta(x)-u_t^{\text {target }}(x \mid z)\right\|^2\right]\]
<p>非常巧妙的是，上述两个损失函数的差其实是一个常数，从而二者梯度相等。这说明只要优化下式，就可以达成优化上式的目的。 证明是将 2 范数展开，这里略过。</p>
<p><strong>例 1</strong> 将上一节中得到的 $u_t$ 高斯标准形式代入本节：</p>
\[\begin{aligned}
\mathcal{L}_{\mathrm{CFM}}(\theta) &amp; =\mathbb{E}_{t \sim \mathrm{Unif}, z \sim p_{\mathrm{data}}, x \sim \mathcal{N}\left(\alpha_t z, \beta_t^2 I_d\right)}\left[\left\|u_t^\theta(x)-\left(\dot{\alpha}_t-\frac{\dot{\beta}_t}{\beta_t} \alpha_t\right) z-\frac{\dot{\beta}_t}{\beta_t} x\right\|^2\right] \\
&amp; \stackrel{(i)}{=} \mathbb{E}_{t \sim \mathrm{Unif}, z \sim p_{\mathrm{data}}, \epsilon \sim \mathcal{N}\left(0, I_d\right)}\left[\left\|u_t^\theta\left(\alpha_t z+\beta_t \epsilon\right)-\left(\dot{\alpha}_t z+\dot{\beta}_t \epsilon\right)\right\|^2\right]
\end{aligned}\]
<p>考虑若 $\alpha_t = t, \beta_t = 1 - t$ 则 $\dot{\alpha}_t = 1, \dot{\beta}_t = -1$, 得到的损失函数形式其实就是 Rectified Flow:</p>
\[\mathcal{L}_{\mathrm{cfm}}(\theta)=\mathbb{E}_{t \sim \mathrm{Unif}, z \sim p_{\text {data }}, \epsilon \sim \mathcal{N}\left(0, I_d\right)}\left[\left\|u_t^\theta(t z+(1-t) \epsilon)-(z-\epsilon)\right\|^2\right]\]
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260317102232349.png" alt=""/></p>
<p><strong>例 2</strong> 直接代入正常分布之间的条件向量场，会发现同样可以得到 Rectified Flow. 这说明这一工具并不要求初始分布的形式。</p>
<h2 id="本章总结">本章总结</h2>
<p>流匹配的目的是学习边际向量场。为此，</p>
<ul>
<li>首先定义了条件概率路径和其对应的条件向量场，从概率路径得到向量场是现实的，并（很可能）有封闭形式；</li>
<li>随后，可以通过条件向量场积分得到边际向量场的形式。</li>
<li>最后由两个 loss 之间的相互转化，可以发现学习条件向量场 $\approx$ 学习边际向量场，这样就可以写出算法了。</li>
</ul>
<h1 id="score-function-and-score-matching">Score Function and Score Matching</h1>
<p>本节讨论如何使用<strong>score matching</strong>来训练 Diffusion Model（这里实在是觉得 score 不知道翻译成什么好）。对分布 $q(x)$, 其 score function 被定义为 $\nabla_x \log q(x)$. 直观上，score function 中每个点的值对应分布 $q(x)$ 中该点沿分布“上升”的方向：</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260317124100794.png" alt=""/></p>
<p>对上一节中定义的边际概率路径 $p_t(x)$ 和条件概率路径 $p_t(x \mid z)$, 其 score function 间有如下关系：</p>
\[\begin{aligned}
\nabla_x \log p_t(x) &amp;= \frac{1}{p_t(x)} \nabla_x p_t(x) \\
&amp;= \frac{1}{p_t(x)} \int_z  \nabla_x p_t (x \mid z) p_{\text{data}}(z) dz \\
&amp;= \int_z \nabla_x \log p_t(x \mid z) \frac{p_t (x \mid z) p_{\text{data}}(z)}{p_t(x)} dz \\
&amp;= \mathbb{E}_{z \sim p_{\text{data}}(\cdot \mid x)} [\nabla_x \log p_t(x \mid z)]
\end{aligned}\]
<p>对比一下 <strong>边际化技巧</strong> \eqref{eq:margin}，会发现形式上的一致性。</p>
<h2 id="例p_textinit--mathcaln0-i_d--p_textdata">例：$p_{\text{init}} = \mathcal{N}(0, I_d),\  p_{\text{data}}$</h2>
<p>上面讨论过的 $p_t(x \mid z): \mathcal{N}(\alpha_t z, \beta_t^2 I_d)$, 由正态分布概率公式可以得到</p>
\[\nabla \log p_t(x \mid z)=\nabla \log \mathcal{N}\left(x ; \alpha_t z, \beta_t^2 I_d\right)=-\frac{x-\alpha_t z}{\beta_t^2}\]
<p>可以发现关于 $x, z$ 都是线性的，这与之前得到的条件向量场是匹配的（即，也是线性）。下面的性质说：<strong>在 gaussian 概率分布下，</strong> 得到了 score function $\Leftrightarrow$ 得到对应的向量场</p>
\[\begin{aligned} u_t^{\text {target }}(x \mid z) &amp; =a_t \nabla \log p_t(x \mid z)+b_t x, \quad a_t=\left(\beta_t^2 \frac{\dot{\alpha}_t}{\alpha_t}-\dot{\beta}_t \beta_t\right), \quad b_t=\frac{\dot{\alpha}_t}{\alpha_t} \\ u_t^{\text {target }}(x) &amp; =a_t \nabla \log p_t(x)+b_t x .\end{aligned}\]
<p>下面的性质也说明，此时学习一个去噪器（即：预测最后的干净数据的期望）和学习 $u_t^{\text{target}}$ 也是等价的：</p>
\[D_t(x \mid z)=z, \quad D_t(x)=\int z \frac{p_t(x \mid z) p_{\text {data }}(z)}{p_t(x)} \mathrm{d} z \stackrel{(i)}{=} \frac{1}{\dot{\alpha}_t \beta_t-\alpha_t \dot{\beta}_t}\left(\beta_t u_t^{\text {target }}\left(x_t\right)-\dot{\beta}_t x_t\right)\]
<p><strong>SDE 采样</strong>. SDE 应当不改变边际概率路径。幸运的是，下面的<strong>转化引理</strong>（名字我自己编的），可以通过引入随机性<strong>但不改变边际概率路径</strong>的方式，达成这一目的：对 $X_0 \sim p_{\text {init }}$,</p>
\[\begin{aligned} \mathrm{d} X_t &amp; =u_t^{\text {target }}\left(X_t\right) \mathrm{d} t+\frac{\sigma_t^2}{2} \nabla \log p_t\left(X_t\right) \mathrm{d} t+\sigma_t \mathrm{~d} W_t \\ &amp; =\left[u_t^{\text {target }}\left(X_t\right)+\frac{\sigma_t^2}{2} \nabla \log p_t\left(X_t\right)\right] \mathrm{d} t+\sigma_t \mathrm{~d} W_t \\ &amp;\Rightarrow \quad X_t \sim p_t(0 \leq t \leq 1) . &amp; \end{aligned}\]
<p>特别地，对 Gaussian 概率路径，由于 score function 和向量场之间可以互相转化，因此只需要 score 即可，有如下形式：</p>
\[\begin{aligned} &amp; X_0 \sim p_{\text {init }}, \quad \mathrm{d} X_t=\left[\left(a_t+\frac{\sigma_t^2}{2}\right) \nabla \log p_t\left(X_t\right)+b_t X_t\right] \mathrm{d} t+\sigma_t \mathrm{~d} W_t \\ \Rightarrow &amp; X_t \sim p_t \quad(0 \leq t \leq 1)\end{aligned}\]
<p>对一般情况，（可能）还是需要同时拟合 score 和向量场。拟合完毕后，使用 Euler-Maruyama Method 进行离散采样即可。</p>
<h2 id="score-matching">Score Matching</h2>
<p>上面一节说明了，对 Gaussian 概率路径，score function 和向量场是可以相互转化的。对其他情况，当然也可以设计一个神经网络 $s_t^{\theta}: \mathbb{R}^{d} \times [0, 1] \to \mathbb{R}^{d}$, 然后用以下两个损失函数来训练：</p>
\[\begin{aligned} \mathcal{L}_{\mathrm{SM}}(\theta) &amp; =\mathbb{E}_{t \sim \text { Unif, } z \sim p_{\text {data }}, x \sim p_t(\cdot \mid z)}\left[\left\|s_t^\theta(x)-\nabla \log p_t(x)\right\|^2\right] \\ \mathcal{L}_{\mathrm{CSM}}(\theta) &amp; =\mathbb{E}_{t \sim \text { Unif, } z \sim p_{\text {data }}, x \sim p_t(\cdot \mid z)}\left[\left\|s_t^\theta(x)-\nabla \log p_t(x \mid z)\right\|^2\right]\end{aligned}\]
<p>这两个损失函数之间也差的是一个常数，因此拿下式做训练也是正确的，一般情况下下式也更好训练。例如，对于 Gaussian 概率路径：</p>
\[\begin{aligned} \mathcal{L}_{\mathrm{CSM}}(\theta) &amp; =\mathbb{E}_{t \sim \text { Unif }, z \sim p_{\text {data }}, x \sim p_t(\cdot \mid z)}\left[\left\|s_t^\theta(x)+\frac{x-\alpha_t z}{\beta_t^2}\right\|^2\right] \\ &amp; \stackrel{(i)}{=} \mathbb{E}_{t \sim \text { Unif }, z \sim p_{\text {data }}, \epsilon \sim \mathcal{N}\left(0, I_d\right)}\left[\left\|s_t^\theta\left(\alpha_t z+\beta_t \epsilon\right)+\frac{\epsilon}{\beta_t}\right\|^2\right] \\ &amp; =\mathbb{E}_{t \sim \text { Unif }, z \sim p_{\text {data }}, \epsilon \sim \mathcal{N}\left(0, I_d\right)}\left[\frac{1}{\beta_t^2}\left\|\beta_t s_t^\theta\left(\alpha_t z+\beta_t \epsilon\right)+\epsilon\right\|^2\right]\end{aligned}\]
<p>这符合 DDPM 的训练方法。</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260317143617167.png" alt=""/></p>
<h2 id="本章总结-1">本章总结</h2>
<ul>
<li>
<p>首先知道了，一个跟随向量场的 ODE 采样过程，可以往其中加入 score function 让其成为边际分布不变的采样过程；</p>
</li>
<li>
<p>其次知道了，在任何情况下，都可以直接训练一个神经网络学习 score function，这个过程学习的是条件 score function, 被叫做 score matching.</p>
</li>
</ul>
<h1 id="guidance-how-to-condition-on-a-prompt">Guidance: How To Condition on a Prompt</h1>
<p>直观的想法是：直接在 $p_{\text{data}}(z \mid y)$ 上采样不就可以了吗（也就是说，把 $y$ 当作某个条件输入 Flow Model）？在这种情况下，训练目标为</p>
\[\mathcal{L}_{\mathrm{CFM}}^{\text {guided }}(\theta)=\mathbb{E}_{(z, y) \sim p_{\text {data }}(z, y), t \sim \operatorname{Unif}[0,1], x \sim p_t(\cdot \mid z)}\left\|u_t^\theta(x \mid y)-u_t^{\text {target }}(x \mid z)\right\|^2\]
<p>然而这样做效果并不好，猜测是有以下两个因素影响：</p>
<ol>
<li>
<p>模型欠拟合，也就是【模型并没有强化 $y$ 对应的真正的语义】；</p>
</li>
<li>
<p>数据和条件并没有完全对齐（数据比较脏）。</p>
</li>
</ol>
<p>因此需要强化条件本身才能做到 guidance.</p>
<h2 id="classifier-guidance">Classifier Guidance</h2>
<p>此处简单起见，以高斯概率路径为例。回顾一下边际 score function 和边际向量场之间的关系，事实上加入条件 $y$ 之后也是如此（可以看作条件分布），因此</p>
\[u_t^{\operatorname{target}}(x \mid y)=a_t \nabla \log p_t(x \mid y)+b_t x\]
<p>之前的无条件生成训练出了 $\nabla \log p_t(x)$，从而直观的想用 $p_t(x \mid y)$ 用贝叶斯公式展开，来利用已经得到的量：</p>
\[\begin{aligned}
u_t^{\operatorname{target}}(x \mid y)&amp;=a_t \nabla \log p_t(x \mid y)+b_t x \\
&amp;= a_t (\nabla \log (\frac{p_t(x) p_t(y \mid x)}{p_t (y)})) + b_t x \\
&amp;= a_t (\nabla \log p_t(x) + \nabla \log p_t (y \mid x)) + b_t x \\
&amp;= u_t^{\text{target}} (x) + a_t \nabla \log p_t (y \mid x)
\end{aligned}\]
<p>倒数第二步是因为 $p_t(y)$ 不会因为 $x$ 变化而改变。得到该式后，直观的想法是用个缩放因子 $w$ 来调整 $a_t$，改变引导强度：</p>
\[\tilde{u}_t(x \mid y)=u_t^{\text {target }}(x)+w a_t \nabla \log p_t(y \mid x)\]
<p>这需要训练一个分类器 $C$，输入 $x, t, y$, 输出 $\log p_t (y \mid x)$. 然而这种方法已经完全被 Classifier Free Guidance 碾压了，所以细节略过不表。</p>
<h2 id="classifier-free-guidance">Classifier-Free Guidance</h2>
<p>如果不需要 $p_t (y \mid x)$, 就只能用原有的形式吗？非也。假如说训练时我们已经训练出了 $u_t^{\text{target}}(x \mid y)$, 那其实我们是知道 $\log p_t (x \mid y)$ 的. 从而，可以对上面的结果再用一次贝叶斯公式：</p>
\[\begin{aligned}
u_t^{\text{target}} (x) + w a_t \nabla \log p_t (y \mid x) &amp;= u_t^{\text{target}}(x) + wa_t \nabla [\log p_t(y) + \log p_t (x \mid y) - \log p_t (x)] \\
&amp;= u_t^{\text{target}}(x) + wa_t \nabla \log p_t (x \mid y) - wa_t \nabla \log p_t(x) \\
&amp;= u_t^{\text{target}}(x) + w u_t^{\text{target}} (x \mid y) - wu_t^{\text{target}}(x) \\
&amp;= (1 - w)u_t^{\text{target}}(x) + wu_t^{\text{target}}(x \mid y)
\end{aligned}\]
<p>其中 $w&gt;1$ 被称作<strong>引导强度</strong>。从几何意义上比较好看出 CFG 的合理性，并且实际效果不错。然而终究只是一个 heuristic, 并没有更理论的证明。因此最终训练时，采用的损失函数为下方函数，其中条件 $y$ 以 $\eta$ 的概率被设为 $\varnothing$.</p>
\[\begin{aligned} \mathcal{L}_{\mathrm{CFM}}^{\mathrm{CFG}}(\theta) &amp; =\mathbb{E}_{\square}\left\|u_t^\theta(x \mid y)-u_t^{\text {target }}(x \mid z)\right\|^2 \\ \square &amp; =(z, y) \sim p_{\text {data }}(z, y), t \sim \operatorname{Unif}[0,1], x \sim p_t(\cdot \mid z), \text { replace } y=\varnothing \text { with prob. } \eta\end{aligned}\]
<p>当然也可以将 guide 得到的向量场进一步加入随机性做扩散采样，但是由于也没有理论依据了，就不展开了。</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260318173754860.png" alt=""/></p>]]></content><author><name></name></author><category term="courses"/><category term="generative"/><summary type="html"><![CDATA[Course note]]></summary></entry><entry><title type="html">Discrete Diffusion Models</title><link href="Marvelim.github.io/blog/2026/mit-6-s184/" rel="alternate" type="text/html" title="Discrete Diffusion Models"/><published>2026-03-16T10:00:00+00:00</published><updated>2026-03-16T10:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/mit-6-s184</id><content type="html" xml:base="Marvelim.github.io/blog/2026/mit-6-s184/"><![CDATA[<p>若要建模的状态是离散的，则很难使用 ODE/SDE 对状态做建模。而下面介绍的 CTMC 则是解决离散问题的有力工具。</p>
<h1 id="preliminaries-continuous-time-markov-chain-ctmc">Preliminaries: Continuous-time Markov Chain (CTMC)</h1>
<p>一个 CTMC 包含如下几部分：</p>
<ol>
<li>
<table>
<tbody>
<tr>
<td>$V$: 词集（如对字母表，$</td>
<td>V</td>
<td>= 26$）；</td>
</tr>
</tbody>
</table>
</li>
<li>
<p>状态空间：$S = V^d$，因此对 $x \in S: x = (v_1, v_2, \ldots, v_d)$.</p>
</li>
<li>速率矩阵: $Q: S \times S \times [0, 1] \to \mathbb{R}<em>{\geq 0}$.  其中 $Q_t(y \mid x)$ 可以被理解为 $t$ 时刻的“瞬时”转移矩阵。对 $Q$ 的限制：$Q_t(y \mid x) \geq 0$; $Q_t(x \mid x) = -\sum</em>{y \neq x} Q_t (y \mid x)$.</li>
</ol>
<p>CTMC 被定义为随机过程 $(X_t)_{t \geq 0}$, 每个 $X_t \in S$ 是随机变量，满足下面的转移概率恒等式：</p>
\[\frac{~\mathrm{d}}{\mathrm{d}h}p(X_{t+h} = y \mid X_t = x) = Q_t(y \mid x)\]
<p>几乎可以理解为把原本无限长的 Markov 过程压缩到了 $t \in [0, 1]$，从而自然满足上述转移条件。从这个恒等式也可以一步推导到很久之后的概率分布。</p>
<h1 id="ctmc-model">CTMC Model</h1>
<p>一个 CTMC Model 定义为被 $\theta$ 参数化的 $Q_t^{\theta}(y \mid x)$. 对这个矩阵有如下特殊限制：若 $y$ 和 $x$ 的汉明距离 $&gt;1$，则 $Q_t^{\theta}(y \mid x) = 0$. 这个限制被叫做 Factorization Condition.</p>
<table>
<tbody>
<tr>
<td>这个限制可以看作是对 $Q_t^{\theta}$ 做降维。本来需要输出 $V^{d} \times V^{d}$ 的状态转移，现在就可以对一个状态 $x$ 和时间 $t$ 直接输出一个<strong>转移矩阵</strong> $(x, t) \to (Q_t^{\theta}(v_i, j \mid x))_{d \times</td>
<td>V</td>
<td>}$, 每个位置代表将 $x$ 的第 $j$ 位修改为 $v_i$ 的概率。</td>
</tr>
</tbody>
</table>
<h2 id="sampling">Sampling</h2>
<p>首先需要一个初始分布 $p_{\text{init}} = \text{Unif}<em>S$. 同时定义 $\delta_y(x) = \mathbf{1}</em>{x = y}$, 和一个采样步长 $h$.</p>
<p>采样过程如下：先采样 $X_0 \sim p_{\text{init}}$，$X_{t+h} \sim p_{t+h \mid t}(y \mid X_t)$. 然而问题在于并不知道这个分布是什么。由于这个过程 $h \to 0$, 对 $h$ 做 Taylor 展开有</p>
\[p_{t+h \mid t}(y \mid X_t) \approx p_{t|t}(y \mid X_t) + h \frac{~\mathrm{d}}{\mathrm{d}h}p(X_{t+h} = y \mid X_t = x)\]
<table>
<tbody>
<tr>
<td>其中 $p_{t</td>
<td>t}(y \mid X_t) = \delta_y(X_t)$, 因此</td>
</tr>
</tbody>
</table>
\[p_{t+h \mid t}(y \mid X_t) \approx
\begin{cases}
1 + hQ_t^{\theta}(X_t \mid X_t), y = X_t \\
hQ_t^{\theta}(y \mid X_t), y \neq X_t
\end{cases}\]
<p>而根据之前对 $Q_t^{\theta}$ 的限制，这也是个概率分布，因此可以直接进行离散采样。</p>
<blockquote>
<p>（btw 我稍微想了下怎么通过该矩阵计算 $Q_t^{\theta}(y \mid X_t)$, 只需要统计一下表格中哪些位置对应该值就可以了，相加即可，对于等于的情况表格里会算重，直接用 $1-$ 其他的就可以。）</p>
</blockquote>
<p>然而如果直接这样采样的话，会发现每次采样只有一个字符发生变化，变化后会重新计算 $Q$. 为了避免这样的低效率，实际操作中采用的是<strong>独立性采样</strong>，也就是根据矩阵，计算出每个位置可能变化为其他字符的概率分布，然后同时采样。</p>
<p>其实感觉这个采样方法引入了挺大的独立性误差，但这个误差带来的好处是可以让每个位置都进行高效的并行化采样，从而减少了 sample $Q$ 的次数、增加了变化的次数。</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260319141552022.png" alt=""/></p>
<p><strong>CTMC Model</strong> 的目标：$X_0 \sim p_{\text{init}} \to X_1 \sim p_{\text{data}}$.</p>
<h2 id="probability-path-to-vector-field-to-flow-matching-loss">Probability Path $\to$ Vector Field $\to$ Flow Matching Loss</h2>
<p>如果能定义采样过程，就可以试图照搬之前对连续生成模型的假设到离散生成模型上了。</p>
<p><strong>条件分布路径</strong> $p_t(x \mid z)$, 满足 $p_0(x \mid z) = p_{\text{init}}(x), p_1(x \mid z) = \delta_z(x)$.</p>
<p><strong>边际分布路径</strong> $p_t(x) = \sum_{z} p_t(x \mid z) p_{\text{data}}(z)$. 这天然满足 $p_0 = p_{\text{init}}, p_1 = p_{\text{data}}$.</p>
<p>下面为条件分布路径设置一个合理的形式。直观的想，对 $(x, z)$, 只需要让 $x$ 逐渐的变化到 $z$ 就可以了，也就是对每个字符 $x_j$ 都逐渐的变化到单点分布 $z_j$. 因此可以使用 schedule ${ \kappa_t }$ 来完成这个混合工作（$\kappa_0 = 0, \kappa_1 = 1$）：</p>
\[p_t(x_j \mid z_j) = (1 - \kappa) p_{\text{init}}^{(j)}(x_j) + \kappa \delta_{z_j}(x_j)\]
<p>既然条件分布路径可以随意设计，那不妨就设计为不同位置独立好了！这样可以得到条件分布路径</p>
\[p_t(x \mid z) = \prod_{j=1}^{d} \left[(1 - \kappa) p_{\text{init}}^{(j)}(x_j) + \kappa \delta_{z_j}(x_j)\right]\]
<p>从该分布中采样的过程也很简单，这里略过。</p>
<p><strong>条件速率矩阵</strong> 若 CTMC 服从这个矩阵，则 $X_t \sim p_t(x \mid z)$.</p>
<p><strong>边际速率矩阵</strong> 若 CTMC 服从这个矩阵，则 $X_t \sim p_t(x)$. 这个矩阵可以使用<strong>边际化技巧</strong>从条件速率矩阵得到。因此这个矩阵才是我们实际需要的矩阵。</p>
<blockquote>
<p><strong>边际化技巧</strong>：定义 \(u_t^{\text {target }}(x)=\int u_t^{\text {target }}(x \mid z) \frac{p_t(x \mid z) p_{\text {data }}(z)}{p_t(x)} \mathrm{d} z,\)，这个向量场满足</p>
</blockquote>
<h2 id="kolmogorov-forward-equation">Kolmogorov Forward Equation</h2>
\[\frac{\mathrm{d}}{\mathrm{d} t} p_t(x)=\sum_{y \in S} Q_t(x \mid y) p_t(y)\]
<p>这个式子成立 <strong>等价于</strong> 跟随 $Q_t$ 采样后，$X_t \sim p_t$. <del>先别问为什么</del></p>
<p>下面证明若条件分布随条件场演化，则边际分布随边际场演化：</p>
\[\begin{aligned}
\frac{d}{dt}p_t(x) &amp;= \sum_{z \in S} \frac{d}{dt} p_t(x \mid z) p_{\text{data}}(z) \\
&amp;= \sum_{z \in S} \sum_{y \in S}  Q_t^{z}(x \mid y) p_t(y \mid z) p_{\text{data}}(z) \\
&amp;= \sum_{y \in S} p_t(y)\sum_{z \in S} \frac{Q_t^{z}(x \mid y) p_t(y \mid z) p_{\text{data}}(z)}{p_t(y)} \\
&amp;= \sum_{y \in S} p_t(y) Q_t(x \mid y)
\end{aligned}\]
<h2 id="构造条件-rate-matrix">构造条件 rate matrix</h2>
<p>只需要对上面的 $p_t(x \mid z)$ 构造 rate matrix $Q_t^{z}$ 满足</p>
\[p_t(x \mid z) = \sum_{y \in S} Q_t^{z} (x \mid y) p_t (y \mid z)\]
<p>虽然我并没有听懂 $Q_t^{z}$ 是如何构造的，但总而言之是可以构造出来的。</p>
<h2 id="从条件-rate-matrix-推导边际-rate-matrix">从条件 rate matrix 推导边际 rate matrix</h2>
\[Q_t\left(v_i, j \mid x\right)=\frac{\dot{\kappa}_t}{1-\kappa_t}\left(p_{1 \mid t}\left(z_j=v_i \mid x\right)-\delta_{x_j}\left(v_i\right)\right)\]
<p>这个式子中唯一需要推导的是 $p_{1\mid t}(z_j = v_i \mid x)$, 这一项就通过神经网络来拟合就好了（即，给定 $x$ 和 $t$, 预测的每一维度是每个字符的概率），使用交叉熵损失进行训练：</p>
\[\mathcal{L}_{\mathrm{DFM}}(\theta)=\mathbb{E}_{z \sim p_{\text {data }}, t \sim \operatorname{Unif}_{[0,1]}, x \sim p_t(\cdot \mid z)}\left[\sum_{j=1}^d-\log p_{1 \mid t}^\theta\left(z_j \mid x\right)\right]\]
<p>这是训练代码：</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260319152636168.png" alt=""/></p>
<h1 id="masked-diffusion-language-model">Masked Diffusion Language Model</h1>
<p>通过引入一个字符 <code class="language-plaintext highlighter-rouge">[MASK]</code>, 这样可以让初始分布直接变成全是 <code class="language-plaintext highlighter-rouge">[MASK]</code>.</p>]]></content><author><name></name></author><category term="courses"/><category term="generative"/><summary type="html"><![CDATA[MIT 6.S184 bonus]]></summary></entry><entry><title type="html">Computer Networks Lab 3</title><link href="Marvelim.github.io/blog/2026/computer-networks-lab-1-copy/" rel="alternate" type="text/html" title="Computer Networks Lab 3"/><published>2026-03-15T10:00:00+00:00</published><updated>2026-03-15T10:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/computer-networks-lab-1%20copy</id><content type="html" xml:base="Marvelim.github.io/blog/2026/computer-networks-lab-1-copy/"><![CDATA[]]></content><author><name></name></author><category term="courses"/><category term="computer_networks"/><summary type="html"><![CDATA[计算机网络（春季）Lab 3 Solution]]></summary></entry><entry><title type="html">Computer Networks Lab 1</title><link href="Marvelim.github.io/blog/2026/computer-networks-lab-1/" rel="alternate" type="text/html" title="Computer Networks Lab 1"/><published>2026-03-15T10:00:00+00:00</published><updated>2026-03-15T10:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/computer-networks-lab-1</id><content type="html" xml:base="Marvelim.github.io/blog/2026/computer-networks-lab-1/"><![CDATA[<h1 id="http-访问解析">HTTP 访问解析</h1>
<h2 id="q1">Q1</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q1</code> 调用的函数 <code class="language-plaintext highlighter-rouge">MySniff</code>。要求：持续抓包 $5$ 秒，结果存为文件 <code class="language-plaintext highlighter-rouge">http.cap</code>（程序中 <code class="language-plaintext highlighter-rouge">Trace1</code> 变量）。</p>
<p><strong>A.</strong> 一个包分为 header（头）和 payload（负载）两部分。使用 <code class="language-plaintext highlighter-rouge">scapy</code> 的 <code class="language-plaintext highlighter-rouge">sniff(timeout = 5)</code> 可以记录一段时间内的所有包。使用 <code class="language-plaintext highlighter-rouge">wrpcap</code> 可以存储为 <code class="language-plaintext highlighter-rouge">cap/pcap</code> 文件。</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">MySniff</span><span class="p">():</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">sniffing start</span><span class="sh">"</span><span class="p">)</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">sniff</span><span class="p">(</span><span class="n">timeout</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>
    <span class="nf">wrpcap</span><span class="p">(</span><span class="n">Trace1</span><span class="p">,</span> <span class="n">packets</span><span class="p">)</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">sniffing stop</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>
<h2 id="q2">Q2</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q2</code>，要求：在 <code class="language-plaintext highlighter-rouge">http.cap</code> 中寻找第一个目的端口为 $80$ 的 <code class="language-plaintext highlighter-rouge">TCP</code> 分组，并返回该分组的源 <code class="language-plaintext highlighter-rouge">MAC</code> 地址和目的 <code class="language-plaintext highlighter-rouge">MAC</code> 地址（字符串格式）。<br/>
注：该分组即第一个 <code class="language-plaintext highlighter-rouge">HTTP</code> 请求分组。</p>
<p><strong>A.</strong> 需要知道一个 packet 的层级结构。可以表示为：</p>
<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p1 = [ Ethernet Header ] + [ IP Header ] + [ TCP Header ] + [ HTTP Data ]
</code></pre></div></div>
<p>例如：</p>
<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>###[ Ethernet ]###
  dst       = 2e:ca:16:e5:5b:64
  src       = 6e:e4:ce:74:a1:f3
  type      = IPv4
###[ IP ]###
     version   = 4
     ihl       = 5
     tos       = 0x0
     len       = 52           &lt;-- 变化1：总长度增加了
     id        = 1234
     flags     = DF
     frag      = 0
     ttl       = 64
     proto     = tcp
     chksum    = 0x84fb
     src       = 192.168.64.2
     dst       = 110.242.70.57
     \options   \
###[ TCP ]###
        sport     = 50894
        dport     = https
        seq       = 4016742112
        ack       = 203847291    &lt;-- 变化2：通常会有确认号
        dataofs   = 5
        reserved  = 0
        flags     = PA           &lt;-- 变化3：标志位改变
        window    = 64240
        chksum    = 0x1234
        urgptr    = 0
        options   = []
###[ Raw ]###                    &lt;-- 变化4：新增了数据层
           load      = 'Hello World!'
</code></pre></div></div>
<p>需要知道的几个事实：</p>
<ol>
<li>
<p>Ethernet 的 src 和 dst: 网卡的 MAC 地址（物理硬件地址），只在同一个物理局域网下有效。</p>
</li>
<li>
<p>IP 的 src 和 dst：绝对 IP 地址。</p>
</li>
<li>
<p>sport 和 dport: 分别开启的发送方 / 接收方端口，用于辨别是传给主机中的哪个程序/进程的。</p>
</li>
<li>
<p>seq: 该包所发送的内容在原始字节流中的位置。比如说有 300 字节的东西，可能 seq 分别为 $X, X+101, X+201$ 这样（X 是因为可能有一个随机的位移，不过这个概念是相对的所以没啥影响）</p>
</li>
</ol>
<p>对一个包 p1, 调用 <code class="language-plaintext highlighter-rouge">TCP in p1</code> 可以判断是否有 TCP 层，<code class="language-plaintext highlighter-rouge">p1[TCP]</code> 可以把除了 TCP 以外的其他 header 都剥开，因此代码为</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Q2</span><span class="p">():</span>
    <span class="n">src_mac</span> <span class="o">=</span> <span class="sh">""</span>
    <span class="n">dst_mac</span> <span class="o">=</span> <span class="sh">""</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">rdpcap</span><span class="p">(</span><span class="n">Trace1</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">p1</span> <span class="ow">in</span> <span class="n">packets</span><span class="p">:</span> <span class="c1"># Ethernet
</span>        <span class="k">if</span> <span class="n">TCP</span> <span class="ow">in</span> <span class="n">p1</span> <span class="ow">and</span> <span class="n">p1</span><span class="p">[</span><span class="n">TCP</span><span class="p">].</span><span class="n">dport</span> <span class="o">==</span> <span class="mi">80</span><span class="p">:</span>
                <span class="n">src_mac</span> <span class="o">=</span> <span class="n">p1</span><span class="p">.</span><span class="n">src</span>
                <span class="n">dst_mac</span> <span class="o">=</span> <span class="n">p1</span><span class="p">.</span><span class="n">dst</span>
                <span class="k">break</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="k">return</span> <span class="n">src_mac</span><span class="p">,</span> <span class="n">dst_mac</span>
</code></pre></div></div>
<h2 id="q3">Q3</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q3</code>，要求：返回 <code class="language-plaintext highlighter-rouge">Q2</code> 中分组对应的应答分组（<code class="language-plaintext highlighter-rouge">ACK</code> 分组）的二层 <code class="language-plaintext highlighter-rouge">type</code> 字段、三层 <code class="language-plaintext highlighter-rouge">proto</code> 字段（整数格式）以及报文时间戳。</p>
<p><strong>A.</strong> 首先要知道什么是 $A$ 的 <code class="language-plaintext highlighter-rouge">ACK</code> 分组 $B$。这个分组就像回执一样，是 D 发给 S 的，证明自己【已经收到了对应内容】。因此这个包的 src, dst IP 和 sport, dport 必须和原包完全反转。除此之外 $B$ 必须要包含 ACK 标志。</p>
<p>这并不足够，因为不知道是回应谁的。对此，B 包还有一个 ack 量，其量应当等于 <code class="language-plaintext highlighter-rouge">A.seq + len(A)</code>, 代表“下一次我接收内容应该是接收这个字节流”。其中，<code class="language-plaintext highlighter-rouge">len(A)</code> 代表 raw data 的字节数，也就是 IP 层总长度 - IP header 长度 - TCP header 长度。</p>
<p>然而如果是 SYN (Synchronize，同步序列号) 或 FIN (Finish，完成发送任务) 包的时候 len(A) 要 $+1$，这可以由标记位确定。由此，可以写出以下代码</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Q3</span><span class="p">():</span>    
    <span class="n">theType</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">theProto</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">theTs</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">rdpcap</span><span class="p">(</span><span class="n">Trace1</span><span class="p">)</span>
    <span class="n">ip_ref</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="n">tcp_ref</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">for</span> <span class="n">p_cur</span> <span class="ow">in</span> <span class="n">packets</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">TCP</span> <span class="ow">in</span> <span class="n">p_cur</span> <span class="ow">and</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">TCP</span><span class="p">].</span><span class="n">dport</span> <span class="o">==</span> <span class="mi">80</span><span class="p">:</span>
            <span class="n">ip_ref</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">IP</span><span class="p">]</span>
            <span class="n">tcp_ref</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">TCP</span><span class="p">]</span>
            <span class="k">break</span>

    <span class="n">expected_ack</span> <span class="o">=</span> <span class="n">tcp_ref</span><span class="p">.</span><span class="n">seq</span> <span class="o">+</span> <span class="nf">len</span><span class="p">(</span><span class="nf">bytes</span><span class="p">(</span><span class="n">tcp_ref</span><span class="p">.</span><span class="n">payload</span><span class="p">))</span>
    <span class="k">if</span> <span class="n">tcp_ref</span><span class="p">.</span><span class="n">flags</span> <span class="o">&amp;</span> <span class="mh">0x02</span><span class="p">:</span>
        <span class="n">expected_ack</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="k">if</span> <span class="n">tcp_ref</span><span class="p">.</span><span class="n">flags</span> <span class="o">&amp;</span> <span class="mh">0x01</span><span class="p">:</span>
        <span class="n">expected_ack</span> <span class="o">+=</span> <span class="mi">1</span>

    <span class="k">for</span> <span class="n">p_cur</span> <span class="ow">in</span> <span class="n">packets</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">TCP</span> <span class="ow">in</span> <span class="n">p_cur</span><span class="p">:</span>
            <span class="n">ip_layer</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">IP</span><span class="p">]</span>
            <span class="n">tcp_layer</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">TCP</span><span class="p">]</span>
            <span class="nf">if </span><span class="p">(</span><span class="n">ip_layer</span><span class="p">.</span><span class="n">src</span> <span class="o">==</span> <span class="n">ip_ref</span><span class="p">.</span><span class="n">dst</span> <span class="ow">and</span> <span class="n">ip_layer</span><span class="p">.</span><span class="n">dst</span> <span class="o">==</span> <span class="n">ip_ref</span><span class="p">.</span><span class="n">src</span><span class="p">)</span> <span class="ow">and</span> \
            <span class="p">(</span><span class="n">tcp_layer</span><span class="p">.</span><span class="n">sport</span> <span class="o">==</span> <span class="n">tcp_ref</span><span class="p">.</span><span class="n">dport</span> <span class="ow">and</span> <span class="n">tcp_layer</span><span class="p">.</span><span class="n">dport</span> <span class="o">==</span> <span class="n">tcp_ref</span><span class="p">.</span><span class="n">sport</span><span class="p">)</span> <span class="ow">and</span> \
            <span class="n">tcp_layer</span><span class="p">.</span><span class="n">ack</span> <span class="o">==</span> <span class="n">expected_ack</span><span class="p">:</span>
                    <span class="n">theType</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">.</span><span class="nb">type</span>
                    <span class="n">theProto</span> <span class="o">=</span> <span class="n">ip_layer</span><span class="p">.</span><span class="n">proto</span>
                    <span class="n">theTs</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">.</span><span class="n">time</span>
                    <span class="k">break</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="k">return</span> <span class="n">theType</span><span class="p">,</span> <span class="n">theProto</span><span class="p">,</span> <span class="n">theTs</span>
</code></pre></div></div>
<h1 id="校园网数据分析">校园网数据分析</h1>
<p>分析一段校园网捕获的记录。</p>
<h2 id="q4">Q4</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q4</code>，要求返回记录中分组数量。</p>
<p><strong>A.</strong> return len 即可。</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Q4</span><span class="p">():</span>
    <span class="n">theLength</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">rdpcap</span><span class="p">(</span><span class="n">Trace2</span><span class="p">)</span>
    <span class="n">theLength</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">packets</span><span class="p">)</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="k">return</span> <span class="n">theLength</span>
</code></pre></div></div>
<h2 id="q5">Q5</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q5</code>，要求返回记录中 <code class="language-plaintext highlighter-rouge">IP</code> 分组、<code class="language-plaintext highlighter-rouge">TCP</code> 分组、<code class="language-plaintext highlighter-rouge">UDP</code> 分组的数量。</p>
<p><strong>A.</strong> 使用 <code class="language-plaintext highlighter-rouge">TCP in p1 / UDP in p1</code> 即可</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Q5</span><span class="p">():</span>
    <span class="n">num_tcp</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">num_udp</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">num_ip</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">rdpcap</span><span class="p">(</span><span class="n">Trace2</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">p_cur</span> <span class="ow">in</span> <span class="n">packets</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">IP</span> <span class="ow">in</span> <span class="n">p_cur</span><span class="p">:</span>
            <span class="n">num_ip</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="k">if</span> <span class="n">TCP</span> <span class="ow">in</span> <span class="n">p_cur</span><span class="p">:</span>
                <span class="n">num_tcp</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="k">elif</span> <span class="n">UDP</span> <span class="ow">in</span> <span class="n">p_cur</span><span class="p">:</span>
                <span class="n">num_udp</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="k">return</span> <span class="n">num_ip</span><span class="p">,</span> <span class="n">num_tcp</span><span class="p">,</span> <span class="n">num_udp</span>
</code></pre></div></div>
<h2 id="q6">Q6</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q6</code>，要求返回记录中 <code class="language-plaintext highlighter-rouge">TCP</code> 流的数量。<br/>
注意：一个 <code class="language-plaintext highlighter-rouge">TCP</code> 流由服务器 <code class="language-plaintext highlighter-rouge">IP</code> 和 <code class="language-plaintext highlighter-rouge">Port</code> 以及客户端 <code class="language-plaintext highlighter-rouge">IP</code> 和 <code class="language-plaintext highlighter-rouge">Port</code> 唯一标识，会产生双向分组，但双向分组属于同一个流，不记为不同流。<br/>
例如，一个 <code class="language-plaintext highlighter-rouge">TCP</code> 流某方向的分组首部为</p>
\[\langle IP_1, IP_2, Port_1, Port_2 \rangle\]
<p>返程分组首部为</p>
\[\langle IP_2, IP_1, Port_2, Port_1 \rangle\]
<p>两者计入同一个 <code class="language-plaintext highlighter-rouge">TCP</code> 流，不认为是两个流。</p>
<p><strong>A.</strong> 只需要去重一下即可</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Q6</span><span class="p">():</span>
    <span class="n">flows</span> <span class="o">=</span> <span class="nf">set</span><span class="p">()</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">rdpcap</span><span class="p">(</span><span class="n">Trace2</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">p_cur</span> <span class="ow">in</span> <span class="n">packets</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">IP</span> <span class="ow">in</span> <span class="n">p_cur</span> <span class="ow">and</span> <span class="n">TCP</span> <span class="ow">in</span> <span class="n">p_cur</span><span class="p">:</span>
            <span class="n">ip_layer</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">IP</span><span class="p">]</span>
            <span class="n">tcp_layer</span> <span class="o">=</span> <span class="n">p_cur</span><span class="p">[</span><span class="n">TCP</span><span class="p">]</span>
            <span class="n">ip1</span><span class="p">,</span> <span class="n">ip2</span><span class="p">,</span> <span class="n">port1</span><span class="p">,</span> <span class="n">port2</span> <span class="o">=</span> <span class="n">ip_layer</span><span class="p">.</span><span class="n">src</span><span class="p">,</span> <span class="n">ip_layer</span><span class="p">.</span><span class="n">dst</span><span class="p">,</span> <span class="n">tcp_layer</span><span class="p">.</span><span class="n">sport</span><span class="p">,</span> <span class="n">tcp_layer</span><span class="p">.</span><span class="n">dport</span>
            <span class="k">if</span> <span class="n">ip1</span> <span class="o">&gt;</span> <span class="n">ip2</span><span class="p">:</span>
                <span class="n">ip1</span><span class="p">,</span> <span class="n">ip2</span> <span class="o">=</span> <span class="n">ip2</span><span class="p">,</span> <span class="n">ip1</span>
                <span class="n">port1</span><span class="p">,</span> <span class="n">port2</span> <span class="o">=</span> <span class="n">port2</span><span class="p">,</span> <span class="n">port1</span>
            <span class="n">flows</span><span class="p">.</span><span class="nf">add</span><span class="p">((</span><span class="n">ip1</span><span class="p">,</span> <span class="n">ip2</span><span class="p">,</span> <span class="n">port1</span><span class="p">,</span> <span class="n">port2</span><span class="p">))</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="k">return</span> <span class="nf">len</span><span class="p">(</span><span class="n">flows</span><span class="p">)</span>
</code></pre></div></div>
<h2 id="q7">Q7</h2>
<p><strong>Q.</strong> 完成函数 <code class="language-plaintext highlighter-rouge">Q7</code>，要求返回记录中所有 <code class="language-plaintext highlighter-rouge">IP</code> 分组长度的最小值、中位数、最大值。<br/>
<code class="language-plaintext highlighter-rouge">IP</code> 分组长度定义为</p>
\[L = L_{\text{Ethernet header}} + L_{\text{IP header}} + L_{\text{IP payload}} + L_{\text{Ethernet trailer}}\]
<p>提示：<code class="language-plaintext highlighter-rouge">IP</code> 分组有 <code class="language-plaintext highlighter-rouge">length</code> 字段，查询其语义并计算上述长度。</p>
<p>补充：有同学反映，小于 $64$ 字节的以太网帧会被自动填充到 $64$ 字节，对于长度计算是否包括填充部分存在疑问；本实验中以 <code class="language-plaintext highlighter-rouge">scapy</code> 的计算方式为准，不需要增加额外判断逻辑。</p>
<p><strong>A.</strong> 如果直接使用 <code class="language-plaintext highlighter-rouge">p_cur[IP].length</code> 会漏算首尾的 Ethernet length. However, 漏算的部分等于 $18$：</p>
<ol>
<li>
<p>目的 MAC + 源 MAC + type: $6 + 6 + 2 = 14$ 字节</p>
</li>
<li>
<p>尾部：FCS （帧检验序列），$4$ 字节</p>
</li>
</ol>
<p>所以直接把所有值 $+18$ 即可。</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">Q7</span><span class="p">():</span>
    <span class="n">min_length</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">max_length</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">median_length</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="c1">#################################
</span>    <span class="c1">###### start of your code #######
</span>    <span class="c1">#################################
</span>    <span class="n">packets</span> <span class="o">=</span> <span class="nf">rdpcap</span><span class="p">(</span><span class="n">Trace2</span><span class="p">)</span>
    <span class="n">len_vec</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">p_cur</span> <span class="ow">in</span> <span class="n">packets</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">IP</span> <span class="ow">in</span> <span class="n">p_cur</span><span class="p">:</span>
            <span class="n">len_vec</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">p_cur</span><span class="p">[</span><span class="n">IP</span><span class="p">].</span><span class="nb">len</span> <span class="o">+</span> <span class="mi">18</span><span class="p">)</span>
    <span class="n">min_length</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">len_vec</span><span class="p">)</span>
    <span class="n">max_length</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">len_vec</span><span class="p">)</span>
    <span class="n">median_length</span> <span class="o">=</span> <span class="n">statistics</span><span class="p">.</span><span class="nf">median</span><span class="p">(</span><span class="n">len_vec</span><span class="p">)</span>
    <span class="c1">#################################
</span>    <span class="c1">###### end of your code #########
</span>    <span class="c1">#################################
</span>    <span class="k">return</span> <span class="n">min_length</span><span class="p">,</span> <span class="n">median_length</span><span class="p">,</span> <span class="n">max_length</span>
</code></pre></div></div>
<p>做完啦！</p>]]></content><author><name></name></author><category term="courses"/><category term="computer_networks"/><summary type="html"><![CDATA[计算机网络（春季）Lab 1 Solution]]></summary></entry><entry><title type="html">计算机网络（春季）第二讲：物理层</title><link href="Marvelim.github.io/blog/2026/computer-networks-physics/" rel="alternate" type="text/html" title="计算机网络（春季）第二讲：物理层"/><published>2026-03-15T10:00:00+00:00</published><updated>2026-03-15T10:00:00+00:00</updated><id>Marvelim.github.io/blog/2026/computer-networks-physics</id><content type="html" xml:base="Marvelim.github.io/blog/2026/computer-networks-physics/"><![CDATA[<h1 id="概念">概念</h1>
<ul>
<li>信号：承载数据的物理量。</li>
</ul>
<p>有模拟信号（连续）和数字信号（离散）两种信号。</p>
<ul>
<li>信道：用来表示<strong>向某一个方向传播信息</strong>的媒体。</li>
</ul>
<p>有单向通信（单工），双向交替（半双工）和双向同时（全双工）三种信道。</p>
<ul>
<li>
<p>码元（code）：使用时间域的波形表示数字信号时、代表不同离散值的基本波形。</p>
</li>
<li>
<p>信号功率 $S$: 单位 W (Watt).</p>
</li>
<li>
<p>信道带宽 $B$: 信号能够不严重失真地，传递信号的<strong>最高频率</strong>，单位 Hz. 有的时候给出的是一个区间，此时带宽是频率区间的跨度。例如 $[3MHz, 4MHz]$: 1MHz.</p>
</li>
<li>
<p>噪声功率 $N$: 线路上平均噪声功率。</p>
</li>
<li>
<p>信噪比 SNR: 单位 dB</p>
</li>
</ul>
\[\text{SNR} = 10 \log_{10} (S / N)\]
<ul>
<li>
<p>数据率：单位时间内传输的比特数，单位 bps</p>
</li>
<li>
<p>误码率：数据传输错误概率，用于衡量传输质量，单位 $1 \times 10^{-n}$.</p>
</li>
</ul>
<h1 id="基本数学和物理学原理">基本数学和物理学原理</h1>
<blockquote>
<p><strong>Fourier 分析</strong>：一个周期 $T$ 的信号可以表示为基频 $1/T$ 的 $n$ 次谐波分量的叠加（即，所有函数的频率都是 $1/T$ 的倍数）：[    g(t) = \frac{1}{2}c + \sum_{n=1}^{+\infty} [a_n \sin (2\pi n f t) + b_n \cos (2 \pi n ft)] ]</p>
</blockquote>
<p>例如，要表示一个字符 <code class="language-plaintext highlighter-rouge">b</code>, 其 ASCII 为 $(01100010)_2$, 可以画图展示出每个分量的时域和频域表示（每个频率的幅度 $\sqrt {a_n^2 + b_n^2}$）：</p>
<p><img src="https://pic-1301372829.cos.ap-hongkong.myqcloud.com/20260316135316458.png" alt="fig"/></p>
<p>信道带宽 $B$ 越高，信号越保真。因此有一个规律性的东西，就是基频 $\times$ 谐波数应该 $=$ 带宽（正好可以表示最高频的分量）。</p>
<blockquote>
<p><strong>Nyquist 定理</strong>：理想无噪声信道条件下，最大数据率 $=2B \log_{2} V$, 其中 $V$ 为信号状态数.</p>
</blockquote>
<p>理解方式：若信道带宽 $B$, 说明频率最快变化是 $B$ Hz. 而波有波峰和波谷，这说明最多可以发 $2B$ 个信号（这只是一个直观理解，有一个 Nyquist 采样定理是真实的说法）。然而其中，一个信号可以携带很多信息，例如振幅、相位等，被叫做信号的<strong>状态数</strong>。因此发送一个信号可以看作携带了 $\log_{2} V$ 比特的信息，因此最大数据率为 $2B \log_2 V$.</p>
<blockquote>
<p><strong>Shannon 公式</strong>：信道极限传输速率 $C = B \log_2 (1 + S/N)$ bps.</p>
</blockquote>
<p>直观理解，信噪比越大，信号越清晰，单个信号可以表达的状态越多。</p>]]></content><author><name></name></author><category term="courses"/><category term="computer_networks"/><summary type="html"><![CDATA[计算机网络（春季）第二讲：物理层 课程笔记]]></summary></entry></feed>